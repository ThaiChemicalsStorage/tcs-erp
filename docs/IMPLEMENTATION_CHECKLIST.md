# Implementation Checklist

> Created 2026-07-10 per an explicit request for a full ERP completion audit. Statuses:
> **Completed** / **In Progress** / **Missing** / **Blocked**. Update this file whenever a status
> changes — it must never go stale. This is a status summary; see [PROJECT_STATUS.md](./PROJECT_STATUS.md)
> for the narrative version and [TODO.md](./TODO.md) for the prioritized backlog these Missing
> items feed into.

## How to read this file

Every "Missing" or "In Progress" row already has a matching entry in [TODO.md](./TODO.md) — this
file is the audit view (what exists vs. not, module by module); TODO.md is the action view
(what to do about it, prioritized). Don't duplicate detail here that TODO.md already owns.

## Core modules

| Module | Status | Notes |
|---|---|---|
| Auth (Setup Wizard + Sign in) | **Completed** | bcrypt + JWT httpOnly cookie, server-verified, no public self-signup. No login rate limiting (see Security Hardening below). |
| Dashboard | **Completed against the full business spec + an independent Codex review + a UI/UX pass + 3 KPI-layout simplification passes + a data/filter-correctness fix pass + a P'Keng/P'Kee business-requirement completion pass**, verification **In Progress** | Full Executive BI rebuild 2026-07-10 (~20 KPIs, pipeline, filters, rankings, forecast, follow-ups, approvals, activity feed, charts), code-reviewed (10 real bugs found and fixed), a same-day completion pass against the detailed business spec, a third pass fixing an independent Codex review's findings, then a **fourth same-day pass** that replaced the Sales Pipeline funnel chart (overlapping labels, read as broken) with horizontal step cards + a separate off-ramp row, added a Quotation Status Summary and a new Sales Activity Analytics section (Created/Edited counts by week/month/quarter/year), dropped 5 now-redundant charts, added shared `PageHeader`/`EmptyState`/`MetricInfoTooltip` components plus an optional first-time Driver.js guided tour — and also split the flat KPI grid into a tiered Primary/Secondary KPI hierarchy. **2026-07-13, third pass**: that KPI tiering was reverted back to a single flat `KpiGrid.tsx`. **2026-07-13, fourth pass**: the flat 22-card grid was itself replaced by a 4-card `ExecutiveSummaryCards` + 2 new compact panels (`SalesPerformancePanel`, `ActivityFollowUpSummary`). **2026-07-13, fifth pass**: `SalesActivityAnalytics` promoted from supporting detail into the top overview per a fully-specified 7-section order, `SalesPerformancePanel` gained Active/Non-Active Quotations. **2026-07-13, sixth pass**: fixed 4 High Priority data/filter-correctness bugs an independent review found (zero Critical) — Sales Activity now tracks all 5 requested event categories (was 2), trend/unfiltered widgets now visibly label their filter exceptions instead of silently ignoring parts of the filter bar, and `QuotationStatusSummary`'s count/value population mismatch is fixed at the source (`api/dashboard/index.ts` now computes value sums with the same predicate as their counts). **2026-07-13, seventh pass**: reorganized to the exact 5-row business-requirement layout (Executive Summary → Quotation Status → Sales Activity Analytics → Recent Activity Details, `SalesPerformancePanel`/`ActivityFollowUpSummary`/forecast chart moved to supporting detail); added helper captions to all 4 KPI cards; added a genuinely new per-salesperson Sales Activity breakdown table (`salesActivity.bySalesperson`); rebuilt `ActivityTimeline` as a table with clickable quotation-number links, backed by new structured `relatedQuoteId`/`relatedCustomerName` audit-log fields; reconfirmed the Win/Lose/Active/Non-Active status mapping stays as-is (user decision) despite the requirement doc proposing a conflicting mapping. **2026-07-13, eighth pass**: a follow-up independent review of the seventh pass found zero Critical issues and 3 High Priority ones — fixed 2: `QuotationStatusSummary`'s Won/Lost/Active/Non-Active donut/table was double-counting Lost quotations (in both the Lose row and the Non-Active row, so the 4 rows' percentages didn't sum to 100%), fixed by excluding Lost from `NON_ACTIVE_OUTCOME_STATUSES` (Lost already has its own row); and the rolling-trend widgets (`SalesActivityAnalytics`/`RevenueTrendChart`) now state their actual anchor date instead of a generic disclaimer. The third finding (department filtering is a fragile free-text join) was deliberately left as an open, already-tracked business-decision TODO item, not fixed this pass. See rows below and CHANGELOG.md. Every other 2026-07-10 addition (pipeline steps, shared components, guided tour, ranking/analytics tables, actionable approval/follow-up lists) stayed unchanged across all six fix/simplification passes. See [MODULES/Dashboard.md](./MODULES/Dashboard.md) for the full itemized list. Not yet exercised against live data in a browser — see [PROJECT_STATUS.md](./PROJECT_STATUS.md) "Known Risks" for the specific, reproducible, three-times-confirmed environment limitation (MongoDB Atlas SRV DNS resolution blocked in the sandboxed session), not a defect; the redesign's client bundle was confirmed error-free via a Playwright check as a partial substitute. **2026-07-14, ninth pass**: every Dashboard monetary total (KPI cards, Status Summary, rankings, job-type/customer analytics, forecast, revenue trend, CSV export) is now pre-tax (before VAT) instead of `Quote.amount`'s VAT-included grand total — `preTaxAmount(amount) = amount / 1.07` (`api/dashboard/index.ts`), exact rather than approximate since `VAT_RATE` has always been a single fixed 7%. All affected Thai/English labels now say "ก่อนภาษี"/"(Before VAT)" explicitly. The 4 required KPI cards, Win/Lose/Active/Non-Active status summary, and weekly/monthly/quarterly/yearly Sales Activity Analytics with a salesperson filter were already in place from the 2026-07-13 passes — this pass's scope was strictly the amount-calculation rule and its labels. Verified via `tsc`/`lint`/`build` (clean) and a temporary isolated Playwright preview (same sandboxed-session DB limitation as every prior pass, see PROJECT_STATUS.md). See [MODULES/Dashboard.md](./MODULES/Dashboard.md) "Pre-Tax Amount Rule." **2026-07-14, tenth pass**: an independent Codex review of the ninth pass found 1 Critical + 3 High Priority issues, all fixed same day — Sales Activity Analytics was gated behind `auditLog:view` (Critical, hid the required section from every default role except full audit-log holders — now gated only by `dashboard:view`); Sales Activity ignored the date-range filter's `from` entirely (High — now bounded via `bangkokDayBoundsUtc`, with the caption switching between "rolling trend" and "filtered" text via a new `dateFiltered` prop); an empty database hid the required KPI cards behind one full-page empty state (High — now a compact inline banner, required sections always render). Also fixed a folded-in Medium finding (Expected Sales now uses strict `isPotentialOpportunity === true`, not truthy). The remaining High finding (no soft-delete predicate) was resolved via documentation — confirmed no such field exists on `Quote` anywhere, so a filter would be dead code; documented instead. `tsc`/`lint`/`build` clean; verified via a Playwright preview specifically simulating a non-`auditLog:view` role. See CODEX_REVIEW_REPORT.md "Claude Fix Status." **2026-07-14, eleventh pass**: pre-tax calculation reworked from `preTaxAmount(amount) = amount / 1.07` (backing out the persisted VAT-included total) to `computeQuoteAmountBeforeVat(lines, discount)` (new shared `api/_lib/quoteAmounts.ts`, computed directly from each quote's line items — the same inputs used to derive the persisted `amount`, never a division of it); `api/dashboard/index.ts`'s 3 raw-amount queries now project `lines`/`discount` instead. Same pass: an audit of all 18 Dashboard monetary widgets found 3 without a "(Before VAT)"/"ก่อนภาษี" qualifier — `SalesPerformancePanel`'s Average Deal Size label, `PipelineSteps`' per-stage value (its `ChartCard` caption now uses the already-correctly-worded but previously empty-state-only `dashboard.pipeline.sub` string), and `FollowUpReminders`' bare `฿` amount (gained a caption) — all fixed; the other 15 widgets already had it. Also: `DashboardPage.tsx`'s page title/description/filter bar now render before the first `/api/dashboard` fetch resolves (extracted a `DashboardContent` subcomponent so only the data-driven widget area shows a first-load skeleton, not the whole page) — see the Progressive Loading row below. **2026-07-14, twelfth pass (independent Codex review fix)**: zero Critical, 3 High + 3 Medium fixed — `GET /api/dashboard`'s 4 optional sections (index creation, activity timeline, sales activity, approval dashboard, notifications) now each fail independently via their own try/catch instead of 500ing the whole response (High #3); the first-load skeleton now shows the real 4 required sections' actual titles/table headers instead of one generic block (Medium); the missing-`lines` pre-tax fallback is now documented + `console.warn`-monitored instead of claimed to never occur (Medium). See CODEX_REVIEW_REPORT.md "Claude Fix Status" and the Progressive Loading row below for the other 2 High Priority fixes (per-page resource gating, refresh indicator). |
| Quotation | **Completed**, real server-side validation + authoritative audit trail + Customer selection | List/create/edit/duplicate, 9-status approval workflow, print/PDF, product-library picker, Job Type/Potential Opportunity/Follow-up Date (added 2026-07-10). **2026-07-10, Codex review fix**: `api/_lib/quoteValidation.ts` validates every field server-side (type/length/range/dates), `amount` is always server-derived (never client-writable), Job Type is required on create and membership-checked on any edit that changes it, quotation numbering is now atomic (`counters` collection, not scan-then-max+1). **2026-07-10, fifth pass**: every mutation (create/update/duplicate/workflow) now writes its own audit-log entry server-side (previously the client called the generic audit endpoint after the fact, which was forgeable); Reject/Customer-Reject/Cancel now require a non-empty comment server-side, not just in the UI. ~~**2026-07-13, eleventh/twelfth pass**: `IssuerCompanySelector`/Company Profile issuer integration~~ — **fully reverted 2026-07-14** (built against a misunderstanding of the requirement; see the Company Profiles row below). **2026-07-14**: replaced with `CustomerSelector` above the Customer Information section — selects a saved [Customer](./MODULES/Customer.md) to autofill it; `customerId` (optional) validated server-side, `customerSnapshot` **always** server-built from the submitted fields (`resolveCustomerIdUpdate()`/`buildCustomerSnapshot()` in `api/handlers/quotes.ts`), customer-link changes locked to Draft status. No fake customer data. **2026-07-14, Codex review fix pass**: fixed 2 High Priority gaps the same review found — `customerSnapshot` was persisted but never displayed (`QuoteDocument.tsx` now seeds every Customer Information field from `quote.customerSnapshot` first, falling back to legacy top-level fields, then empty); and selecting a customer only conditionally overwrote `deliveryMethod`/`project`/`deliveryAddress`, leaving stale values from a prior selection when the new customer's fields were blank (now assigned unconditionally). Also cleaned up dead `IssuerCompanySnapshot`/`IssuerCompanyDisplay` types and stale present-tense issuer documentation the same review flagged as Medium. See CODEX_REVIEW_REPORT.md "Claude Fix Status." **Missing, by design**: no "บันทึกเป็นลูกค้าใหม่" (save manually-typed info as a new Customer, inline from the quotation form) — explicitly optional/future per the requirement. **2026-07-14, same day**: "สร้างใบเสนอราคา / Create Quotation" now opens the new Quotation Templates wizard first (see the row below) instead of jumping straight to a blank form; `QuoteLine` gained an optional `isSectionHeader` field for template-seeded section dividers. |
| Product Library | **Completed**, one gap | CRUD, categories, archive, search/filter/sort/pagination all real. **Missing**: button-level (create/edit/delete) permission gating in the UI — only the sidebar entry respects `products:view` today; the underlying API routes are already properly permission-gated server-side, this is a UX-only gap. |
| Job Type master data | **Completed**, one gap | 13 seeded defaults, `GET/POST/PATCH /api/jobtypes`, wired into Quotation form/list/PDF/Dashboard, now required and server-validated on quote create (2026-07-10, Codex review fix). **Missing**: no dedicated admin UI to add/edit job types beyond the 13 seeded defaults — the API exists (`company:manage`), the page doesn't. |
| User Management | **Completed** | Create/edit/reset password/activate/deactivate/assign role-department-position, last-Super-Admin guards. |
| Role Management | **Completed** | Custom roles, permission matrix editor, system-role locking (Super Admin fully locked, Administrator name-locked only). |
| Notifications | **Completed** | Bell badge (0/count/99+), role-based delivery, mark-read/delete. **2026-07-10, Codex review fix**: click now deep-links to the specific related quotation (a `quotationDeepLinkId` lifted to `App.tsx`), not just the module list. **2026-07-10, fifth pass**: added the 3 missing terminal-workflow notifications (`quotation_won`/`quotation_lost`/`quotation_cancelled`) — `marked_won`/`marked_lost`/`cancelled` previously never notified the quote's creator, unlike every other workflow transition. |
| Audit Log | **Completed**, quotation events now non-forgeable | Append-only, read-only, server-derived actor identity. No period-grouped analytics view (see Activity Analytics below). **2026-07-10, fifth pass**: `POST /api/audit-log` now rejects the `"ใบเสนอราคา"` module — quotation events are written exclusively by `api/handlers/quotes.ts` itself now, closing a forgery gap that let any authenticated caller POST fabricated "Quotation Created"/"Quotation Updated" entries (which the Dashboard's Sales Activity Analytics counted from). Other modules' audit entries (Profile/Company/User/Role changes, Login/Logout) are unchanged, still client-triggered — not flagged by the review, out of scope for this pass. |
| Settings (Profile/Company/Security) | **Completed** | Profile picture + signature upload, company logo/stamp/bank/VAT/T&C, real password change. `Company.vatRate` is stored/editable but **not wired into `computeTotals()`** — see Known Gaps below. |
| Quotation Templates | **Completed (2026-07-14), fixed against an independent Codex review the same day, extended 2026-07-15 with the full Template Management admin module, fixed against a second independent Codex review the same day** | Create Quotation wizard (Job Type → Template → Preview → the normal form, pre-filled). 5 templates extracted from a real Excel workbook into a new `quotation_templates` collection, idempotent SHA-256-hash-gated import, new optional `Quote.quotationTemplateId/Name/Version` + `QuoteLine.isSectionHeader` fields (fully backward-compatible), snapshot-not-live-reference copy-on-apply semantics, section-header divider rendering in the editor/PDF, and a "Template ใบเสนอราคา" Global Search result group. **2026-07-14, same-day Codex-review fix pass**: 0 Critical, 3 High Priority fixed. **2026-07-15, Template Management pass**: built the full admin UI — list + create/edit page, duplicate, activate/deactivate/archive, 7 new granular `quotationTemplates:*` permissions, server-side audit logging, Job Type grid availability badges, permission-gated "create Template for this Job Type" wizard action. **2026-07-15, second Codex-review fix pass**: 0 Critical, 3 High + 3 Medium fixed — real runtime `.xlsx` workbook parsing added (`api/_lib/templateWorkbookParser.ts`, `xlsx` re-added as a production dependency) computing a genuine per-sheet content hash so a workbook edit is now detectable (full row-classification automation deliberately deferred — see module doc for the concrete ambiguous-row evidence); `Quote.templateSnapshot` added (real structured copy of sections/terms/internal-notes/source-hash, frozen at creation, never rendered anywhere); `applyTemplateToQuoteDraft()` now copies `item.subDetails` and honors `item.visibleToCustomer` (both previously broken); template item product links now server-resolved/server-snapshotted; import race-safe against duplicate-key errors; wizard badges now loading/error-aware. `tsc -b`, `tsc --noEmit -p tsconfig.api.json`, `lint`, `build` all pass clean on all 3 passes; a local dev-server + Playwright check confirmed zero browser console errors each time. **Missing**: no live MongoDB/browser round-trip verification of any pass (same sandboxed-session limitation as every recent pass — no local DB credentials); no full automated row-classification from the parsed workbook (deliberate, see above); no version-history/rollback (a plain `version` string, by design — see the module doc). See [MODULES/QuotationTemplates.md](./MODULES/QuotationTemplates.md) and `docs/CODEX_REVIEW_REPORT.md`'s "Claude Fix Status." |
| Scope of Work | **Completed (2026-07-15), fixed against an independent Codex review the same day** | New document type generated from an existing quotation (reproduces the reference PDF's printed structure), reached via a "สร้าง Scope of Work"/"เปิด / แก้ไข Scope of Work" action on Quotation Detail — no new sidebar module. New `scope_of_works` collection with an independent quotation-derived snapshot (customer/items/Job Type/PO/salesperson), server-generated `PQ{YYYYMM}-{jobSequence}-{jobTypeCode}-{secondaryCode}` scope number via an atomic per-month counter, 11 reusable checklist groups (Job-Type-driven default only for Test Report, per spec), item list copied from the quotation with no pricing ever, blue-handwritten-style fields left blank/editable, Draft/Final lifecycle, ทำสำเนา/อัปเดตข้อมูลจากใบเสนอราคา actions, print/PDF, 6 new `scopeOfWork:*` permissions enforced server-side (owner-or-finalize rule), full audit logging, Global Search integration. API shares `api/handlers/quotes.ts`'s function file — still 12 of 12 Vercel function slots used. **2026-07-15, same-day Codex-review fix pass**: fixed all 3 High Priority issues an independent review found (0 Critical) — `customerSnapshot.contactName` + a new frozen `quotationSalesperson` field now retain real quotation data previously dropped entirely; the default `seller` now prefers the quotation's actual salesperson (with a real-user signature-image lookup) instead of always the creator; `secondaryCode` is now required at creation (collected via a prompt) so the job code always has all 4 segments; `GET /api/search` gained a `scopeOfWorks` result group, closing the "no Global Search integration" gap. Also fixed (Medium): explicit A4 `@page` size, item+detail rows grouped into one unbreakable `<tbody>` each, `refresh` now requires `quotations:view` too, bounded duplicate-key retry on create/duplicate, corrected delete-confirmation wording. `tsc -b`, `tsc --noEmit -p tsconfig.api.json`, `lint`, `build` all pass clean on both passes. **Missing**: no live deployment/browser verification yet (same sandboxed-session limitation as every recent pass); `secondaryCode`'s business *meaning* is still an explicitly open question (only its presence is now enforced); no un-finalize action (ทำสำเนา is the documented workaround); no restore UI for a soft-deleted record. See [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) and `docs/CODEX_REVIEW_REPORT.md`'s "Claude Fix Status." |
| Company Profiles | ❌ **Removed (2026-07-14)** | Built 2026-07-13 (add/edit/view/activate-deactivate/archive/set-default for business-identity master data), briefly mis-wired into the Quotation form as an issuer-company selector, reverted the same week, then the standalone admin module itself removed entirely on 2026-07-14 — this ERP only ever needs one issuer company. UI/routes/permissions/i18n all removed; `company_profiles` MongoDB collection and any existing documents/audit entries deliberately left untouched (no destructive DB cleanup). See [MODULES/CompanyProfiles.md](./MODULES/CompanyProfiles.md). |
| Lead Management | **Schema only — deliberately not built out further** | `leads`/`lead_activities` collections + indexes exist. No API routes/UI. Explicitly re-confirmed 2026-07-10 as out of scope for the current phase (see PROJECT_STATUS.md) — the single largest genuinely-missing piece of the long-term ERP vision, tracked as its own future undertaking, not silently forgotten. |
| Customer Management | **Completed (2026-07-14)** | Redefined the earlier (2026-07-09) unused schema-only `customers` collection to match the Quotation form's actual fields, then built the full module: `CustomersPage.tsx` admin CRUD, `GET/POST /api/customers`, `GET/PATCH /api/customers/:id`, `POST /api/customers/:id/archive`, 4 new permissions, and a `CustomerSelector` on the Quotation form wired to `Quote.customerId`/`customerSnapshot`. Its API briefly shared the `company-profiles` serverless function's slot (Vercel Hobby's 12-function cap); since the Company Profiles module was removed later on 2026-07-14, `api/handlers/customers.ts` now has its own dedicated function file. `customer_contacts` remains schema-only (a single contact per customer is what the Quotation form actually needs). See [MODULES/Customer.md](./MODULES/Customer.md). |
| Progressive/shell-first loading | **Completed (2026-07-14), reworked the same day (Codex review fix)** | `src/App.tsx`'s boot sequence: sidebar/header render as soon as the session check resolves, not after a further 9-way domain-data fetch also completes — each of those 9 fetches now updates its own state independently. Fixed a previously-documented gap: the boot session-check had no error handling (a thrown network error left the app stuck loading forever) — now a retryable `BootError` screen. **2026-07-14, Codex review fix (High #1)**: the original version still gated every prop-driven page (Quotations/Products/Customers/Users/Roles/Settings) behind one shared `initialDataLoading` flag covering all 9 resources — replaced with per-resource `resourceStatus` tracking + a `NAV_RESOURCES` map naming which resources each page actually needs, so e.g. navigating to Products no longer waits on unrelated `notifications`/`quotes`/`users`. `loadDomainData`/`trackResource` wrapped in `useCallback` (with a module-level `INITIAL_RESOURCE_STATUS` constant) so the boot effect can list `loadDomainData` as a dependency without re-running on every render. `DashboardPage.tsx`: title/description/filter bar render before the first `/api/dashboard` fetch resolves; the data-driven widget area (extracted into `DashboardContent`) shows a first-load skeleton that, as of the Codex fix pass (Medium), mirrors the real 4 required sections' titles/table headers instead of one generic block. **2026-07-14, Codex review fix (High #2)**: filter-change/retry already correctly kept previous data visible with a small spinner, but the Approval Dashboard widget's post-approve/reject refresh (`refreshAfterAction`) did not — it silently updated `stats` with zero visible indication; now also sets `loading`, surfacing a "กำลังอัปเดตข้อมูล..." label next to the page title. **Missing**: no per-widget progressive rendering within an already-loaded Dashboard; Quotations/Products/Users/Roles/Customers/Settings still have no independent *internal* loading state of their own beyond the page-level `SectionLoading` placeholder (deliberately not redesigned — see UI_GUIDELINES.md). See [UI_GUIDELINES.md](./UI_GUIDELINES.md) "Progressive/Shell-First Loading" and PROJECT_STATUS.md. |
| Department-based filtering | **Completed on Dashboard (free-text join), still blocked elsewhere** | 2026-07-10: Dashboard gained a real Department filter, resolved server-side to "every salesperson whose free-text `User.department` matches" — works today, but inherits the same name-variation caveat as customer-name matching until a real `Department` entity exists. No other module filters by department yet; that would need the same foundational work as Lead/Customer Management. |
| Activity Analytics (period-grouped, multi-dimension) | **Completed** (2026-07-10, extended 2026-07-13 sixth + seventh passes) | `SalesActivityAnalytics.tsx` covers period-grouped (week/month/quarter/year) activity across 5 event categories (Created/Edited/Status Changed/Approval Requested/Approval Completed, sixth pass), plus a `(period, salesperson)` breakdown table for Created/Edited (seventh pass, `salesActivity.bySalesperson`) — matches the business requirement's "ช่วงเวลา × พนักงานขาย" table exactly. `ActivityTimeline` (Recent Activity Details) remains a flat recent-N feed by design (that's the requirement's spec for it — a chronological list, not a grouped analytics view), now with clickable quotation links and customer names (seventh pass). |
| Report Export (PDF/Excel/CSV) | **Missing — explicitly deferred twice** | No CSV/Excel/PDF export exists anywhere in the app beyond the existing browser-print quotation PDF. Deferred by explicit user choice both when the Dashboard was originally scoped and again in this pass, to be built against the now-stable dashboard response shape. |

## Cross-cutting concerns

| Area | Status | Notes |
|---|---|---|
| RBAC enforcement | **Completed** | Server-side on every mutating route via `requirePermission()`/a local `requireAnyPermission()` helper for OR-style checks, genuinely unbypassable via devtools. 29-permission model (6 `companyProfiles:*` permissions added 2026-07-13, removed 2026-07-14 with the rest of that module; 4 `customers:*` permissions added 2026-07-14, not Super-Admin-locked; 8 `quotationTemplates:*` permissions — `:manage` added 2026-07-14, `:view/create/edit/duplicate/activate/archive/import` added 2026-07-15 — not Super-Admin-locked), 6 default roles. Users can't edit their own role; can't self-deactivate as last Super Admin. |
| MongoDB as sole business-data source | **Completed** | No hardcoded/mock business data anywhere in the app (confirmed during the 2026-07-09 production-readiness pass and re-confirmed by this session's code review — the fake `salesTeam.ts` sample module and 5 fake Dashboard sections were removed 2026-07-09, nothing has reintroduced hardcoded business data since). |
| Empty states | **Completed** | Dashboard, Products, Quotations, Notifications, Audit Log all have real Thai/English empty-state copy distinguishing "genuinely empty" from "no results for this filter." |
| i18n (Thai/English) | **Completed** | ~530 dictionary keys, essentially all UI chrome covered. Persisted data/seed content and the printed quotation document are Thai-only by deliberate design, documented as such, not a gap. |
| Security hardening | **Missing (3 items)** | No rate limiting on `POST /api/auth/login`; MongoDB Atlas credential rotation recommended 2026-07-09, still unconfirmed; bcrypt cost factor (10) not explicitly tuned. See RBAC.md Known Gaps. |
| Quote payload / upload validation | **Completed** | 2026-07-10, Codex review fix: `api/_lib/quoteValidation.ts` (quote fields, always-server-derived `amount`, Job Type membership) and `api/_lib/uploadValidation.ts` (profile picture/signature/company logo/stamp — MIME type + 2MB size cap). Previously: quote writes copied raw client fields with no validation at all; uploads accepted arbitrary strings. |
| Automated tests | **Missing** | Zero test coverage anywhere in the repo — frontend or the API layer. Flagged as high-priority since day one of the backend migration, still not started. |
| CI pipeline | **Missing** | No typecheck/lint/build gate on push — real risk since GitHub auto-deploys to production on push to `master`. |
| GitHub → Vercel auto-deploy | **Completed, verified** | Confirmed via Vercel MCP tooling — the production deployment matches the latest `master` commit. |
| Global Search (topbar) | **Completed (2026-07-14), fixed against an independent Codex review the same day** | Replaces a previously non-functional decorative search input (no `value`/`onChange`, generic-template placeholder). `GET /api/search?q=` (`api/_lib/searchHandler.ts`) searches Quotations/Customers/Products (view-permission each), a static permission-filtered pages/menu list, and Users (`users:manage`) — every category independently RBAC-filtered server-side. `src/components/GlobalSearch.tsx`: debounced 300ms results, keyboard nav (arrows/Enter/Escape), Ctrl/Cmd+K shortcut, previous results stay visible during refetch, loading/empty/error states, deep-links directly into each record's edit form (new `initialEditId` prop on Customers/Products/UserManagement pages, mirroring Quotation's existing `initialQuoteId`). New indexes via `ensureSearchIndexes()`. **2026-07-14, Codex review fix pass (zero Critical, 2 High + 2 directly-relevant Medium fixed)**: added a server-side `MAX_QUERY_LENGTH` (100 chars, was unbounded — a real DoS/performance risk on the multi-collection regex scan) plus a matching client `maxLength`; added a real `lg:hidden` mobile/tablet entry point (full-screen search takeover reusing the same state as the desktop dropdown — previously invisible below 1024px, with Ctrl/Cmd+K silently focusing a hidden input); Customer search results now respect the caller's own `customers:edit` permission before deep-linking into the editable form (previously bypassed the same gate the list UI's edit button already enforces — Products left unchanged, it has no equivalent gate at all today, a separate pre-existing issue); added combobox/listbox ARIA semantics + active-row scroll-into-view. See CODEX_REVIEW_REPORT.md "Claude Fix Status." **Missing/deliberately deferred**: recent-search history (explicitly optional per the requirement), MongoDB Atlas Search (full collection scans per category are acceptable at this ERP's real data volume — documented limitation, not a defect), automated test coverage (this project has no test infrastructure anywhere, a separate longstanding decision), English quotation-status search aliases (Low Priority, not fixed). See [API.md](./API.md)/[DATABASE.md](./DATABASE.md)/[RBAC.md](./RBAC.md)/[UI_GUIDELINES.md](./UI_GUIDELINES.md) "Global Search." |
| Vercel Hobby function-count headroom | **Exhausted (12 of 12)** | `api/handlers/company-profiles.ts` (2026-07-13) was the last available slot; it briefly hosted `/api/customers` too (dispatched on the raw pathname) before both the Company Profiles module and that file were removed 2026-07-14, freeing the slot for `api/handlers/customers.ts` to become its own dedicated file — still 12 of 12 used. **2026-07-14**: `api/handlers/customers.ts` now also dispatches `/api/search` (Global Search) the same way, still 12 of 12 — no new file added. **2026-07-14, second same-day pass**: `api/handlers/jobtypes.ts` now also dispatches `/api/quotation-templates*` the same way, still 12 of 12 — no new file added. Any future new API resource needs a new dispatch branch inside an existing handler, not a new file — see ARCHITECTURE.md/API.md/TODO.md. |
| Live-data verification of 2026-07-10 work | **In Progress, environment-blocked** | See Dashboard row above — `tsc`/`lint`/`build` clean across all three 2026-07-10 passes, code-reviewed, not yet browser-tested against real data. Both the second and third passes' sessions confirmed why: their sandboxed network can't resolve MongoDB Atlas's SRV DNS record (`querySrv ECONNREFUSED`), reproduced on an untouched pre-existing route each time, so no session running in that same environment can close this out — needs either a different network environment or a Vercel preview-deployment click-through. |
| Sidebar header overflow / typography | **Completed** | 2026-07-13: fixed `BrandMark.tsx`'s wordmark overflow bug (`whitespace-nowrap` with no wrap/ellipsis fallback), added `Noto Sans Thai` font fallback globally (body + all Playfair Display headings), raised base line-height to 1.6, fixed 4 tables missing `overflow-x-auto`. Verified via an isolated Playwright preview harness (full app blocked by the same no-live-backend limitation as the row above). See CHANGELOG.md and UI_GUIDELINES.md. |
| Codex UI/UX review — Critical/High fixes | **Completed** | 2026-07-13, second same-day pass. An independent Codex review of the row above found 3 Critical (no mobile sidebar, topbar overflow below ~900px, unqualified 2-column quotation-editor grids) + 5 High (sidebar brand-title width margin, dense/mono-heavy typography incl. a latent `font-mono`-never-wired bug, Dashboard density, truncated text with no `title=` fallback, non-wrapping page headers) issues. All fixed: real off-canvas mobile drawer (`md:` breakpoint, backdrop, Escape-to-close), progressive topbar disclosure (`sm:`/`lg:` breakpoints, replacing the earlier md: cutoff that still wrapped the user's name into 4 lines at ~800px), every flagged form grid converted to `grid-cols-1 sm:grid-cols-2` (quotation editor, User/Role Management, Setup Wizard), centralized `--font-sans`/`--font-mono` design tokens, a collapsible Dashboard secondary-KPI section (**superseded 2026-07-13, third pass** — see row below, the collapsible mini-card section it refers to was later deleted), `title=` tooltips on every flagged truncation, `flex-wrap` page headers on 5 pages. `tsc`/`lint`/`build` clean; verified via a second isolated Playwright preview harness across 390–1440px. See CHANGELOG.md and UI_GUIDELINES.md. **Missing** (Medium/Low, explicitly deferred, see CHANGELOG.md "Not done"): decorative topbar search, hover-only notification delete, non-functional "Remember me", non-semantic notification rows (`div onClick` not `button`), no `focus-visible` audit. |
| Dashboard KPI hierarchy revert | **Completed, superseded by the row below** | 2026-07-13, third same-day pass. Direct user feedback: the 2026-07-10 redesign's tiered KPI cards (6 large hero cards + 9 dense mini-cards) "feels strange, too template-like." User confirmed (clarifying question asked first) they wanted the KPI *presentation* restyled back to a single flat grid while keeping every current Dashboard section working — not a full technical revert that would have dropped Sales Activity Analytics/Quotation Status Summary. `PrimaryKpiCards.tsx`/`SecondaryKpiSummary.tsx` deleted, replaced by one flat `KpiGrid.tsx` (uniform card size, all 22 `DashboardKpis` fields — 7 more than were being shown, since the redesign had silently stopped rendering Lose Rate/Conversion Rate/Average Approval Time/Expired Quotations/New+Repeat Customers/Total Leads despite the API still computing them). `QuotationStatusSummary`/`PipelineSteps`/`SalesActivityAnalytics`/charts/tables untouched. `tsc`/`lint`/`build` clean; verified via an isolated Playwright preview of the real component at 1440px/390px. See CHANGELOG.md, UI_GUIDELINES.md, MODULES/Dashboard.md. |
| Dashboard overview simplification (5 compact sections) | **Completed, superseded by the row below** | 2026-07-13, fourth same-day pass. Further user feedback that even the flat 22-card `KpiGrid.tsx` above still felt cluttered with no hierarchy — explicit request for exactly 4 primary KPI cards + compact panels across 5 named sections. `KpiGrid.tsx` deleted; replaced by `ExecutiveSummaryCards.tsx` (4 cards only), `SalesPerformancePanel.tsx` (new — 6 rate/cycle-time metrics as a compact grid), `ActivityFollowUpSummary.tsx` (new — 4 action-item counts, Pending Approvals clickable). `QuotationStatusSummary.tsx` gained a Percentage column. 4 fields (`totalCustomers`/`totalLeads`/`totalProducts`/`repeatCustomers`) dropped from individual tiles — not in the requested spec, still computed server-side, visible via `CustomerAnalytics.tsx`/Products page instead. Page reorganized: the 5 requested sections now lead the page in the requested order; every pre-existing section (charts, pipeline, rankings, actionable approval/follow-up lists, notification summary) moved below an "In-Depth Detail" divider, content unchanged. Filter bar padding/control sizing tightened. `tsc`/`lint`/`build` clean; verified via an isolated Playwright preview of the real components at 1440px/390px. See CHANGELOG.md, UI_GUIDELINES.md, PROJECT_STATUS.md. |
| Dashboard 7-section reorder | **Completed** | 2026-07-13, fifth same-day pass. A fully-specified follow-up naming the exact 7 top-level sections + order wanted (Header+Filters, Executive Summary, Quotation Status, Sales Performance, Sales Activity Analytics, Tasks/Follow-up/Approvals, Recent Activities). `SalesActivityAnalytics` (component itself unchanged) promoted from "supporting detail" into the top overview, positioned between Sales Performance and Tasks/Follow-up. `SalesPerformancePanel.tsx` gained Active/Non-Active Quotations (8 metrics total, was 6). Expected Sales helper text reworded to lead with the requested phrase while keeping the "regardless of status" accuracy qualifier. Re-verified already-true items: exact title/subtitle match, old funnel chart stays gone (`PipelineSteps.tsx` untouched since 2026-07-10), sidebar/font fixes unchanged (`git diff --stat` confirmed), every section already has its own empty-state guard. `tsc`/`lint`/`build` clean; verified via an isolated Playwright preview of all 7 real section components in the requested order, at 1440px/390px. See CHANGELOG.md, UI_GUIDELINES.md, PROJECT_STATUS.md. |
| Dashboard data/filter-correctness fix (Codex High Priority) | **Completed** | 2026-07-13, sixth same-day pass. An independent follow-up Codex review found zero Critical issues and 4 High Priority data/filter-correctness bugs, all fixed: (1) `SalesActivityAnalytics` now tracks all 5 requested event categories via `categoryForAction()` in `api/dashboard/index.ts` (was Created/Edited only), rendered as a **stacked** bar chart (not 5 grouped bars, which would triple the bar count); (2) its and `RevenueTrendChart`'s "rolling window ignores the filter's start date" behavior — a deliberate, documented design so trend charts don't collapse to 1-2 points on a narrow date filter — is now stated in the UI (`sub` text) instead of silent; (3) `ProductsByCategoryChart`/`NotificationSummary`/the forecast win-rate baseline (all deliberately not fully filter-scoped, for documented business reasons) now say so explicitly too; (4) `QuotationStatusSummary`'s count/value population mismatch fixed at the source — `api/dashboard/index.ts` now computes `lostValue`/`activeQuotationsValue`/`nonActiveQuotationsValue` with the *exact same* predicate as their matching counts (new `DashboardKpis` fields), so the component no longer approximates from the `pipeline` prop (dropped from its API). No business/filtering logic removed — item 4 fixed a real bug, items 2–3 only added labels. `tsc` (frontend + `tsconfig.api.json`), `lint`, `build` all clean; verified via an isolated Playwright preview of the real components with mock data at 1440px/390px. **Not done** (Medium/Low per the review): Overdue Follow-ups/Expired Quotations tiles still not clickable (no matching filter exists); supporting-detail area still requires scrolling; Recent Activities' "related record" still free text; inline Playfair/color style objects not consolidated. See CHANGELOG.md, UI_GUIDELINES.md, PROJECT_STATUS.md, TODO.md. |

## Explicitly out of scope for this phase (not bugs, not oversights)

- Separate `quotation_items`/`quotation_approvals`/`quotation_status_history`/`sales_activities`/`followups` collections — deliberately embedded/reused instead (`Quote.lines`/`Quote.approvalHistory`, `audit_log`, `Quote.followUpDate`), reconfirmed 2026-07-10. See [DATABASE.md](./DATABASE.md) "Deliberately not built as separate collections."
- Full Lead/Customer/Department entity build-out — reconfirmed 2026-07-10, see above.
- Sequential two-level approval (Approver L1 → L2) — no concrete business need surfaced yet.
- Dark mode — `theme.css` only defines the light palette.
- Real blob storage for uploads (still base64-in-document, 16MB MongoDB ceiling) — `uploads`/`attachments` collections exist as schema-only prep.

## Estimated completion

~40% of the full long-term ERP vision (HR/Accounting/Inventory/Warehouse/Purchasing/Project
Management not started; Lead/Customer schema-only). **~97%** of the currently-scoped modules
(Dashboard/Quotation/Product/Auth/Settings/User Mgmt/Role Mgmt/Notifications/Audit Log) —
the Dashboard's remaining gaps against its business spec were closed in the second 2026-07-10
pass, an independent Codex review's Critical/High findings (quote validation, Dashboard
filter honesty, Job Type enforcement, notification deep-link, CSV export, atomic numbering,
upload validation) were closed in the third same-day pass, and that same review's follow-up
addendum findings (quotation audit-trail forgery, missing required-comment/workflow-notification
enforcement) were closed in a fifth same-day pass (see CHANGELOG); the remaining ~3% is
the specific gaps listed above (button-level Product gating, VAT wiring, Job Type admin UI,
Activity Analytics grouping, PDF/Excel export, `GET /api/users` privacy-model decision,
`Quote.salesperson`→real-user-reference migration decision, automated tests/CI), not placeholder
or fake functionality. See [PROJECT_STATUS.md](./PROJECT_STATUS.md) for the maintained narrative
version of this number.
