# TODO

> Grouped by priority. Move items to Completed as they land; add newly discovered items as they're found. See [CLAUDE.md](./CLAUDE.md) for the standing update rule.

## High Priority

- [ ] **Manually verify the 2026-07-31 rolling session expiration against a live/`vercel dev` session.** Verified via `tsc --noEmit`/`npm run lint`/`npm run build`/`npm test` (all clean, 56/56) and code review only — meaningfully verifying "stays alive across 7 days of daily use, dies after 7 days of zero use" isn't practical in a single sandboxed session. Specifically unverified against real usage: the `Set-Cookie` header's `Max-Age`/expiry is visibly sliding forward in devtools on each API call; a session genuinely survives being left open (with periodic activity) past the old fixed 7-day mark; a genuinely abandoned (zero-request) session still 401s exactly at the 7-day mark, not later. See CHANGELOG.md/SESSION_LOG.md 2026-07-31.
- [ ] **Commit the local `vercel dev` MongoDB DNS fix in `api/_lib/mongodb.ts` (2026-07-30, uncommitted).** Local `vercel dev` was failing every request with `querySrv ECONNREFUSED` (Node's own DNS SRV resolution for the `mongodb+srv://` string, blocked by something on the machine's network stack even though the OS resolver worked fine) — fixed with `dns.setServers(["8.8.8.8","1.1.1.1"])` gated on `!process.env.VERCEL_ENV` (never runs in an actual deployment). Tested and confirmed working (`/api/auth/session` 500→200) but not yet committed — needs the user's go-ahead per the standing commit policy. See SESSION_LOG.md 2026-07-30.
- [ ] **Fix `ConfirmDialog`'s `useDialogA11y` call running even while the dialog is closed (found in the 2026-07-30 re-audit).** `src/components/ConfirmDialog.tsx` calls the hook before its `if (!open) return null` guard (unlike `PromptDialog`, which splits into a wrapper + form so the hook only mounts while open) — currently a harmless no-op (Escape fires `onCancel()` globally even when closed, which is a no-op against an already-null state), but it's the exact latent-bug shape worth closing: split `ConfirmDialog` the same way. See the 2026-07-30 re-audit report in this session's transcript.
- [x] ~~**Fix the reintroduced status-pill contrast bug + keyboard-inaccessible rows + hardcoded Thai in the standalone Scope of Work list page (found in the 2026-07-30 re-audit).**~~ — **done 2026-07-30**: `src/pages/scopeOfWork/ScopeOfWorkList.tsx` darkened its local `statusStyle` to the same `#576f94`/`#a75d1a`/`#207e52` formula; table rows gained `tabIndex`/`role="button"`/`onKeyDown`/`aria-label`; `ScopeOfWorkPage.tsx`'s loading skeleton gained `role="status" aria-live="polite"` (error branch gained `role="alert"`); the "ยังไม่มี PO" badge bumped from `text-[10px]` to `text-xs` to match the Status Pill spec; ~90 new `scopeOfWork.*`/`scopeOfWorkDoc.*` i18n keys wired via `t()` across both the list *and* the full `ScopeOfWorkDocument.tsx` (status-label literals, toasts, and checklist content deliberately left hardcoded). A separate, more serious P0 was also found and fixed in the same pass — `ScopeOfWorkDocument.tsx`'s loading/error states had **no way back at all**, unlike `DeliveryOrderDocument.tsx`'s equivalent fix; now has the same back-button toolbar. `ValidationSummary.tsx` (shared by Quotation/Scope of Work/Delivery Order) gained `role="alert"`. Verified live via `vercel dev` + `lint`/`build`/`test` (56/56). See CHANGELOG.md 2026-07-30 "Scope of Work full-workflow re-audit + accessibility fix pass".
- [x] ~~**Fix the reintroduced status-pill contrast bug + keyboard-inaccessible rows + hardcoded Thai in the standalone Delivery Order list page (found in the 2026-07-30 re-audit).**~~ — **done 2026-07-30**: `src/pages/deliveryOrder/DeliveryOrderList.tsx` darkened its local `statusStyle` to the same `#576f94`/`#a75d1a`/`#207e52` formula; table rows gained `tabIndex`/`role="button"`/`onKeyDown`/`aria-label`; `DeliveryOrderPage.tsx`'s loading skeleton gained `role="status" aria-live="polite"`; ~15 new `deliveryOrder.*` i18n keys wired via `t()` (status-label literals deliberately left hardcoded, matching `DeliveryOrderDocument.tsx`'s own precedent). Verified live via `vercel dev` + `lint`/`build`/`test` (56/56). See CHANGELOG.md 2026-07-30.
- [ ] **Product decision needed: should Quotation/Scope of Work/Delivery Order detail views be reflected in the URL?** Surfaced by the 2026-07-30 `/impeccable audit` — there is no router (`App.tsx` only hash-syncs the top-level module nav, not which specific quote/wizard/Scope-of-Work is open within a module), so refreshing the browser while viewing a specific record always drops back to that module's bare list, and Back doesn't step from detail→list. This is a whole-app architectural pattern, not a Quotation-specific bug, and was deliberately left unfixed pending an explicit decision rather than patched locally. See CHANGELOG.md 2026-07-30 and SESSION_LOG.md for the full finding.
- [ ] **Manually verify the 2026-07-30 Quotation/Scope of Work/Delivery Order accessibility pass against a live deployment/browser.** Verified via `tsc`/`lint`/`build`/`npm test` (all clean) and code-level review only — same standing sandboxed-session no-live-browser limitation as every prior pass. Specifically unverified: tabbing through the Customer Info/Document Details panels with a screen reader actually announces each field's label; the new up/down reorder buttons on line-item sub-details and Scope of Work items genuinely move the row and stay keyboard-operable; double-clicking Confirm on a slow network no longer fires a workflow action (finalize/delete/etc.) twice; the status pills on Scope of Work/Delivery Order visually match Quotation's now that they share the darkened-text formula; Escape actually closes `ConfirmDialog`/`PromptDialog`/the workflow-action modal and returns focus sensibly.

- [ ] **⚙️ Server migration — execute the Go-Live Checklist in [SERVER_MIGRATION_PLAN.md](./SERVER_MIGRATION_PLAN.md) when the owner says go.** Recorded 2026-07-24 per direct request; the checklist (steps A–H) is the single complete list — do not re-derive it here. Two sub-items were doable **now**, before any server exists: **(1) verify a company domain with Resend + set `EMAIL_FROM`** (step A — until then, document-recipient emails only deliver to the Resend account owner's own address, the limitation the user hit 2026-07-24) — **still open**; **(2) the 4 pending manual Role Management grants** (step E) — ✅ **done 2026-07-29**, see the completed item below. **Step G (regenerate the user manual PDF)** — ✅ **also done 2026-07-29**, see Completed. Everything else is deferred until development finishes and the owner explicitly starts the migration.
- [x] ~~**Scope of Work attachments (2026-07-24): verify the MongoDB-storage implementation live.**~~ — **verified live same day** via a scripted browser session against production: uploaded a 388 KB Thai-named PDF ("ทดสอบแนบไฟล์ TCS ERP.pdf") through the real UI (success toast + list entry), fetched its capability URL from a plain session-less HTTP client (HTTP 200, all 397,772 bytes, `application/pdf` — proving email recipients can open it with no login), deleted it through the UI, and confirmed the URL then returns 404 (the `scope_attachment_files` document is really gone). Feature history: went through three same-day iterations — (1) Vercel Blob + `BLOB_READ_WRITE_TOKEN` gating (503: newer stores provision OIDC creds instead); (2) OIDC support (failed: the store was private-access, incompatible with email links); (3) final MongoDB storage after the user clarified **the Vercel deployment is only a trial — real hosting will be elsewhere**. **Still unverified (deliberately — sending would spam real recipients): the attachment-links block renders correctly in a real recipient email.** The user's now-unused Vercel Blob store can be deleted. See [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) "Attachments."
- [x] ~~**⚠️ ACTION REQUIRED for Scope of Work's new "ส่งอีเมลแจ้งผู้รับเอกสาร" feature to work at all: set `RESEND_API_KEY` in Vercel environment variables.**~~ — **resolved 2026-07-23, verified live**: the user signed up at resend.com, added `RESEND_API_KEY` to Vercel, and redeployed (confirmed `READY`/`production`, matching the latest commit, via the Vercel MCP tools). Tested via Resend's zero-setup sandbox sender (`onboarding@resend.dev`, which only delivers to the account owner's own confirmed email) against a test user's email and confirmed receipt — the send pipeline genuinely works end-to-end. **Still open**: no custom sending domain is verified yet, so `EMAIL_FROM` still defaults to the sandbox address, which cannot deliver to arbitrary real employee addresses (only the Resend account owner's own confirmed one) — verify a real domain and set `EMAIL_FROM` once the company has one, see [ARCHITECTURE.md](./ARCHITECTURE.md).
- [ ] **Manually verify the new Document Recipients custom message + formal email restyle (2026-07-23) against a live deployment/real inbox.** Verified via `tsc`/`lint`/`build` (all clean) and a standalone Node script confirming the HTML-generation logic (message placement, blank-message omission, multi-line handling, XSS-escaping) — no way to actually send/receive a real test email this session (same `RESEND_API_KEY`-dependent limitation as the original Document Recipients pass, compounded by no live browser). Specifically unverified: the restyled email actually renders correctly (not just as valid HTML) in real inboxes, particularly Gmail and Outlook's web/desktop clients, which are notoriously stricter about inline CSS than the HTML spec technically allows; the gold call-to-action button renders as a clickable, visually obvious button rather than a plain link in a client that strips `background`/`border-radius` from inline styles; a genuinely multi-line custom message displays with real line breaks rather than collapsing.
- [ ] **Manually verify the new "What's New" update-log panel (2026-07-23) against a live deployment.** Verified via `tsc`/`lint`/`build` (all clean) only — no browser check this session (Playwright MCP still disconnected). Specifically unverified: the sparkle icon renders correctly next to the bell at every topbar breakpoint without crowding it out (same responsive constraints documented in UI_GUIDELINES.md "Topbar"); the gold "unseen" dot shows on first login for a user who's never opened it, and correctly clears (and stays cleared across a page reload/re-login) once they open the panel; two different users signed in on the same browser profile don't leak each other's "seen" state (the `localStorage` map is keyed by `User.id`, should be fine, but untested for real).
- [ ] **Manually verify the new auto-generated Revision Note feature (2026-07-23, Quotation + Scope of Work) against a live deployment.** Verified via `tsc`/`lint`/`build` (all clean) and a standalone `tsx` script exercising the diff-generation logic against realistic mock data — same sandboxed-session no-live-browser limitation as every prior pass (Playwright MCP tools disconnected earlier this session and never reconnected). Specifically unverified against real data: the "สร้างสรุปการแก้ไขอัตโนมัติ" button only appears on an actual revision (not the root document); clicking it on a real rewritten Quotation/Scope of Work correctly resolves the immediate predecessor and produces an accurate, readable Thai summary; regenerating after manually editing the note doesn't lose the user's edits until they click the button again (by design); the note persists correctly through a normal save; a revision whose predecessor no longer exists or is inaccessible (deleted, or permission-filtered) shows the "ไม่พบข้อมูลต้นฉบับ" toast instead of crashing.
- [x] ~~**⚠️ ACTION REQUIRED before/immediately after this deploys: grant the 7 new `deliveryOrder:*` permissions to existing production roles.**~~ — **done 2026-07-29**: granted `deliveryOrder:view/viewAll/edit/finalize/print` to Administrator (already had `create`/`delete`), Approver Level 1, and Approver Level 2 via Role Management. **Sales User was deliberately left untouched** — its `deliveryOrder:view/create/edit/print` grant is a separate, older gap not part of this pass (it predates this TODO item and wasn't flagged as newly broken); worth a follow-up check. See CHANGELOG.md.
- [ ] **Manually verify the new Delivery Order module (2026-07-23) against a live deployment/browser.** Verified via `tsc`/`lint`/`build` (all clean) and a standalone Node script confirming the create/refresh snapshot-and-reconcile logic (`deriveItemsFromScope()`/`deriveInstallmentsFromScope()`) — no live browser/database check this session (same standing sandboxed-session limitation as every other pass: no MongoDB credentials, Playwright MCP disconnected). Specifically unverified against real data: creating a Delivery Order from a real Scope of Work correctly pulls customer info/items; the per-installment item checkboxes save and reload correctly; the printed output actually renders as separate physical pages in a real browser print preview (the `break-after: page` CSS was verified as syntactically correct, not against a real rendering engine); the company letterhead renders correctly with a real uploaded logo; "อัปเดตข้อมูลจาก Scope of Work" correctly reconciles installments against real edited payment-schedule data; the standalone sidebar page's list/detail flow and permission gating behave correctly for each of the 5 default roles; that a Scope of Work whose payment schedule includes a "Down Payment" installment genuinely never gets a page for it on a real created/refreshed Delivery Order (2026-07-23 same-day fix, verified only via a standalone Node script so far).
- [ ] **Follow up on the "กดพิมพ์แล้วไม่มีอะไรเลย" report (2026-07-23) — root cause not fully confirmed.** A user reported that ticking an item in the second installment card, then clicking print, produced nothing (later clarified: a "กรุณาเลือกรายการสินค้า..." toast appeared instead of the print dialog). A live interactive reproduction of the exact same sequence, using the real components against a mocked backend, worked correctly (the print gate passed, `window.print()` fired) — found and fixed an unrelated real bug along the way (duplicated "บริษัท บริษัท" in the signature line) but could not reproduce the reported symptom itself. Most likely explanation: the print button was clicked before the checkbox tick had actually registered. Ask the user to retry (tick first, confirm the checkbox visibly shows checked, then click print) and report back if the toast still appears — if it does, that would point at a real, still-undiagnosed state-sync bug worth a deeper look.
- [ ] **Manually verify Scope of Work's new Document Recipients feature (2026-07-23) against a live deployment — partially confirmed already.** The user independently confirmed the core send pipeline (department dropdown → recipient picker → save-then-send → real email received) works in production. Still specifically unverified: a legacy user whose `department` predates the dropdown shows correctly as a fallback option and doesn't silently lose their value on an unrelated save; a pre-2026-07-23 Scope of Work record's `documentsToSend` checklist correctly gains the new "Accounting" option via the extended `withDefaultChecklistGroups()` backfill.
- [ ] **Manually verify the new in-app notification + recipient list visibility (2026-07-23, same-day follow-up pass) against a live deployment.** Added in direct response to the user's own follow-up question ("ทำยังไงถึงให้มันไปโผล่ในหน้า scope of work ของเรา... อยากให้ขึ้นแจ้งเตือนในระบบด้วย") — verified only via `tsc`/`lint`/`build` (all clean) and code review against the already-shipped, already-verified `quotations`-equivalent patterns (identical deep-link/query-filter shapes), no live click-through this session. Specifically unverified against real data: a recipient without `scopeOfWork:viewAll` who didn't create the record genuinely sees it appear on their own Scope of Work list/search after being picked as a recipient; the bell notification actually arrives for that recipient and clicking it correctly jumps straight to the record's detail view (not just the list); the notification fires even when that recipient's own email delivery failed (the two channels are meant to be independent). See [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) "Document Recipients."
- [x] ~~**⚠️ ACTION REQUIRED before/immediately after this deploys: grant `scopeOfWork:viewAll` to existing production roles.**~~ — **done 2026-07-29**: ticked "ดู Scope of Work ของผู้อื่น" for Administrator, Approver Level 1, Approver Level 2, and Viewer via Role Management. See CHANGELOG.md.
- [ ] **Manually verify the new `scopeOfWork:viewAll` permission (2026-07-23) against a live deployment.** Verified via `tsc`/`lint`/`build` only (all clean) — no mock-data harness for this one specifically, since the behavior is a pure server-side MongoDB query filter with no new client-side rendering logic to harness (the list page already renders whatever `fetchAllScopeOfWorks()` returns, unchanged). Specifically unverified against real data: a Sales User (no `scopeOfWork:viewAll`) genuinely only sees their own Scope of Work records on the standalone list page and in Global Search; an Approver (has `scopeOfWork:viewAll`, once manually granted per the item above) still sees everyone's; a legacy/seed record with an empty `createdBy` is visible to a non-`viewAll` user as intended; the by-quotation existence check (`QuoteDocument.tsx`'s toolbar) and direct record `GET` remain correctly unfiltered (a Sales User can still open/duplicate a colleague's Scope of Work reached via a quotation they can see, even without `viewAll`).
- [x] ~~**⚠️ ACTION REQUIRED before/immediately after this deploys: grant `quotations:viewAll` to existing production roles.**~~ — **done 2026-07-29**: ticked "ดูใบเสนอราคาของผู้อื่น" for Administrator (already had it), Approver Level 1, Approver Level 2, and Viewer via Role Management. See CHANGELOG.md.
- [ ] **Manually verify the new `quotations:viewAll` permission + Salesperson filter (2026-07-22) against a live deployment.** Verified via `tsc`/`lint`/`build` (all clean) and a mock-data harness for the Salesperson dropdown's client-side filtering only — same sandboxed-session no-MongoDB-network limitation as every prior pass for the actual server-side enforcement. Specifically unverified against real data: a Sales User (no `quotations:viewAll`) genuinely only sees their own quotations in both the list page and Global Search; an Approver (has `quotations:viewAll`, once manually granted per the item above) still sees everyone's; a legacy/seed quote with an empty `createdByUserId` is visible to a non-`viewAll` user as intended; the Salesperson filter dropdown correctly reflects whatever subset of quotes the current user is allowed to see.
- [ ] **Manually verify the new standalone Scope of Work page (2026-07-22) against a live deployment — one real bug already found and fixed here, worth a careful re-check.** The list page's initial crash-to-blank-white (fixed the same day — missing-field fallbacks + a new app-wide `ErrorBoundary`, see CHANGELOG.md) was caught only because the user actually clicked into it in production; `tsc`/`lint`/`build` and a mock-data harness cannot catch a real-data-shape issue like that one. Specifically still unverified: `GET /api/scope-of-works` (no `quotationId`) actually returns every non-deleted record company-wide with the right fields against the real collection; clicking a list row opens the correct record in detail view; the "กลับไปรายการ Scope of Work" back button returns to this page's list correctly; duplicating a Scope of Work from within the standalone page correctly switches to viewing the new copy; the sidebar nav item is hidden for a role without `scopeOfWork:view` and visible for one with it.
- [ ] **Manually verify Scope of Work's new Rewrite action + Salesperson filter (2026-07-22) against a live deployment.** Verified via `tsc`/`lint`/`build` (all clean) and a mock-data harness for the Salesperson column/dropdown only — same sandboxed-session no-MongoDB-network limitation as every prior pass for the actual server-side Rewrite behavior. Specifically unverified against real data: first rewrite of a Scope of Work produces `{root}-R1`; rewriting that `-R1` produces `-R2` (not `-R1-R1`); `yearMonth`/`jobSequence`/`secondaryCode`/`issueDate` genuinely pass through unchanged from the source; the revision counter is race-free under two near-simultaneous clicks; the source record is provably untouched after a rewrite; the "back to list" fix (now re-fetching) actually shows a just-rewritten/-duplicated record without a manual reload.
- [ ] **Manually verify the Dashboard revision-chain de-duplication fix (2026-07-22) against a live deployment with real rewritten quotation data.** Verified via `tsc`/`lint`/`build` (all clean) and a throwaway-script sanity check of the core dedup logic against a synthetic revision chain — same sandboxed-session no-MongoDB-network limitation as every prior pass. Specifically unverified against real data: creating a rewrite chain (root + 2 revisions) and confirming the Dashboard's total pre-tax value/quotation count/pipeline/salesPerformance/customerAnalytics/jobTypeAnalytics/forecast/follow-ups/repeat-customer classification all count it exactly once using the latest revision's data; confirming a rewrite of a Won quote that resets to Draft correctly drops out of `closedSales`/revenue trend/forecast win rate until (and unless) the new revision itself reaches Won; confirming `activityTimeline` still shows the "Quotation Rewritten" audit event on its own (unaffected by this fix, by design).
- [ ] **Manually verify the new Quotation Rewrite/Revision feature (2026-07-22) against a live deployment/browser.** Verified via `tsc --noEmit` (both tsconfigs)/`lint`/`build` (all clean) and by tracing the logic line-by-line against the already-shipped, equivalent-shape Duplicate action — same sandboxed-session no-MongoDB-network limitation as every prior pass (`vercel dev` also unavailable, Vercel CLI not installed). Specifically unverified against real data: first rewrite of an unsuffixed quote produces `-R1`; rewriting that `-R1` produces `-R2` (not `-R1-R1`); rewriting the original again after `-R1`/`-R2` exist produces `-R3`; the revision counter is genuinely race-free under two near-simultaneous clicks; copied business data (customer/customerSnapshot/lines/templateSnapshot/quantities/prices/discounts/notes/conditions) matches the source exactly; the source quote is provably untouched after a rewrite; an unauthorized user is blocked at both the UI and a direct `POST /api/quotes/:id/rewrite` call; a simulated failure leaves no partial record and keeps the user on the source quote.
- [ ] **Manually verify the 2026-07-21 pinned sub-details restyle against a live deployment/browser**, particularly `TemplateEditorView.tsx`. `LineItemsEditor.tsx`'s version was confirmed working in a temporary local mock-data harness (Pin icon adds a gold-highlighted row, auto-focuses, drag-reorder still works, sticky-note card for Notes/Specifications/Tags unaffected) — see CHANGELOG.md 2026-07-21. `TemplateEditorView.tsx`'s `ItemEditor` is not exported and wasn't independently harnessed (would need substantially more mock props: job type, products, save handlers); it was verified only by static review against the already-confirmed `LineItemsEditor.tsx` pattern (matching colSpan arithmetic, identical Pin/focus/highlight JSX). `tsc`/`lint` both pass clean.
- [x] ~~[2026-07-20 rollback] Re-run `POST /api/quotation-templates/import` against the live database to actually apply the FRP Lining v2.0 rollback to production data.~~ — **resolved 2026-07-20, verified live**: logged into the real production app (https://tcs-erp-nine.vercel.app) as an authenticated Super Admin and inspected `Template ใบเสนอราคา` → `LI-FRP-LINING` directly — it was already the genuine v1.0 content (`Structure layer`/`Operating cost`/`Prepare surface` sub-items, tax note wording, version `1.0`, 17 items in 1 section), with "แก้ไขล่าสุด" (last edited) showing `14 ก.ค. 2569` for every one of the 5 templates. The audit log confirmed the only prior `Templates Imported` events were on `15 ก.ค. 2569` — nothing ran between the FRP v2 deploy and its revert, so the live master record never actually diverged from v1.0 in the first place. Clicked "นำเข้าจาก Excel" (import) live anyway for a formal record: `POST /api/quotation-templates/import` → `200`, and every template's "last edited" timestamp stayed unchanged afterward, confirming the importer's hash-gate correctly detected zero content difference (all 5 reported `skipped`, zero writes). See `docs/CODEX_REVIEW_REPORT.md` "Claude Fix Status" for the full evidence.
- [x] ~~[2026-07-20] Push the Cancel/Back button-visibility rollback commit (`8cfc9fe`) to `origin/master`.~~ — **resolved 2026-07-20**: pushed `8cfc9fe` and `4ec69aa` with the user's explicit go-ahead; Vercel deployment `dpl_AoFreuApxeyb6UvxmLkiDhLD75kB` (commit `4ec69aa`) built and went `READY` on the `production` target. Re-verified live via the same authenticated browser session: an open quotation's back-link button now reports `className: "flex items-center gap-1.5 text-sm text-muted-foreground hover:text-foreground transition-colors"` — the exact restored old style, no border/background/focus-ring — confirming both rollbacks are now fully live, not just committed.
- [x] ~~Quotation required-field validation pass (2026-07-16)~~ — **superseded the same day**: a later, explicit business decision reversed this — Quotation must not force every field to be completed, and its "ข้อกำหนดเอกสารและการส่งมอบ" section is removed entirely. Only `client` remains required (the same single field required before any of this validation work started). See CHANGELOG.md "Quotation: make fields optional again, remove Document Requirements and Delivery." **Scope of Work's own required-field validation from the same original pass is unaffected and still fully enforced** — see the Scope-of-Work-specific items below.
- [ ] **Manually verify the 2026-07-16 Quotation field-relaxation pass against a live deployment/browser.** Verified via `tsc`/`lint`/`build` (all clean) and code-level review only — same sandboxed-session no-live-database limitation as every prior pass. Specifically unverified against real data: Create Quotation with every optional field left empty saves as Draft with no errors; Submit/Approve/Print/Send-to-customer succeed with only `client` filled in; the "ข้อกำหนดเอกสารและการส่งมอบ" section is genuinely gone from Create/Edit/Detail/Print/PDF with no blank card/spacing left behind; a Quotation saved during the ~1-day window the removed feature existed (with a stray `checklistGroups` property in MongoDB) still opens, edits, and saves without error; creating a Scope of Work from a Quotation still works and its checklist groups start at the same unchecked defaults as always (confirming the adjacent `deriveFromQuotation()` fallback fix didn't regress Scope of Work).
- [ ] **Confirm the real Billing Terms / ปจ.2 / Delivery Terms / Document Destination option catalogs with the business — now a Scope of Work-only question.** Scope of Work's own checklist groups (`buildDefaultChecklistGroups()`, `src/lib/documentRequirements.ts`) still use placeholder-ish option sets (Cash/Credit/custom payment schedules, "ต้องดำเนินการ"/"ไม่เกี่ยวข้อง" for ปจ.2, pickup/delivery-after-PO for delivery terms, etc. were never added, per the original spec's "don't auto-select without a confirmed business rule" instruction) — Quotation no longer has this section at all as of the same-day rollback above, so this item applies only to Scope of Work now.
- [ ] **"Scroll to and focus the first invalid field" is only implemented at the summary level** for Scope of Work (Quotation's validation surface shrank to a single `client` field, where this is largely moot) — not per-field programmatic focus. A fuller implementation would need a ref per required field; deliberately out of scope for this pass given the number of fields involved.
- [ ] **Old Scope of Work records won't retroactively gain a newly-added "อื่น ๆ" option inside a checklist group they already have** — `sanitizeChecklistGroups()`/`withDefaultChecklistGroups()` only backfill a wholly-missing *group*, never add a new *option* to a group a record already has. Not expected to affect real data today (the module is one day old as of this pass), but would need a small migration if it ever does.
- [ ] **Manually verify the new Scope of Work feature against a live deployment/browser**, including the 2026-07-15 Codex-review fix pass. Verified via `tsc`/`lint`/`build` (all clean) and code-level review only — same sandboxed-session no-live-database limitation as every prior pass. Specifically unverified against real data: creating a Scope of Work from a real quotation pulls the expected customer/contact/salesperson/item/PO data; the secondary-code prompt + scope-number counter behave correctly end-to-end and are genuinely race-free under concurrent creates in the same month; checklist checkbox/radio state persists correctly across save/reopen; print output renders correctly in an actual browser print preview (incl. the new per-item `<tbody>` grouping actually keeping an item and its detail row together across a real page break); a Sales User genuinely cannot edit/delete another user's Draft while an Approver genuinely can; the new "Scope of Work" Global Search group returns correct results and respects `scopeOfWork:view` for a role that lacks it.
- [x] ~~`secondaryCode`'s business meaning is still unconfirmed~~ — **partially addressed 2026-07-15 (Codex review fix pass)**: presence is now required at creation (closes the "job code always missing its 4th segment" High Priority finding), but the *meaning* of the value itself is still an open business question — see [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) "Open Business Question." Confirm with the business whether it should instead be a selectable list (per site/team/salesperson) rather than free text.
- [ ] **Scope of Work's shipping-contact vs. billing-contact fields never auto-fill** — a `Quote` only has one generic contact, not a delivery-specific and billing-specific pair, so these 4 fields (name/phone × 2) always start blank per the "don't guess" requirement. The generic contact name itself is now at least retained (`customerSnapshot.contactName`, 2026-07-15 fix) rather than dropped. Revisit if `Customer`/`Quote` are ever extended with distinct shipping/billing contacts.
- [ ] **Scope of Work has no un-finalize action** — once a record is `Final` it's permanently locked (only "ทำสำเนา" continues the work from a fresh copy). Confirm this matches the intended business process before it's relied on in practice.
- [ ] **Scope of Work has no restore UI/endpoint for a soft-deleted record** — flagged Low Priority by the 2026-07-15 Codex review; the delete confirmation's wording was corrected the same day to stop implying a restore path exists. Add a real one if this is ever needed in practice.
- [ ] **Scope of Work's checklist groups don't include a separately-named "Contract inspection" group** — the spec's suggested category list mentioned one, but the reference PDF (used as the literal source of truth for exact labels/grouping per the task) has no directly equivalent printed group. Flagged Medium by the 2026-07-15 Codex review. Confirm with the business whether an additional group is actually needed, or whether the existing billing-contract-conditions/payment-conditions groups already cover the intent.
- [x] ~~Topbar search box was decorative — no `value`/`onChange`, generic-template placeholder mentioning purchase orders/SKU/vendors~~ — **done 2026-07-14**: implemented a real, permission-aware Global Search (`GET /api/search`, `src/components/GlobalSearch.tsx`) across Quotations/Customers/Products/pages/Users; placeholder replaced with copy matching this ERP's actual modules. See CHANGELOG.md.
- [ ] **Manually verify Global Search against a live deployment.** Verified this pass (and the 2026-07-14 same-day Codex-review fix pass) via `tsc`/`lint`/`build` (all clean) and code-level review only — same sandboxed-session limitation as every prior pass (no `MONGODB_URI`, no Vercel CLI). Specifically unverified against real data: searching by quotation number/customer name/product SKU actually returns matching MongoDB records; a non-`users:manage` role genuinely never receives `users` results (not just hidden client-side); rapid typing doesn't race a stale response ahead of a newer one; Ctrl/Cmd+K and full keyboard navigation work in a real browser; **the new mobile full-screen search takeover at real device widths** (tablet/phone) — layout, tap targets, and the Ctrl/Cmd+K `matchMedia` breakpoint check have only been verified by code review, not an actual narrow viewport.
- [ ] **Global Search has no recent-search history.** Explicitly optional per the original requirement ("do not implement if it adds significant complexity") — skipped to keep this pass's scope to the core search feature. Could be added later as a small `localStorage`-backed per-user list of recent queries/navigation targets if requested.
- [x] ~~Global Search has no mobile/narrow-viewport UI~~ — **done 2026-07-14** (independent Codex review, High #2): added a real `lg:hidden` icon trigger opening a full-screen search takeover, reusing the exact same search state/logic as the desktop dropdown. Ctrl/Cmd+K now checks `window.matchMedia("(min-width: 1024px)")` to target whichever UI is actually visible, instead of always focusing the (possibly invisible) desktop input. See CHANGELOG.md.
- [ ] **English quotation-status search aliases don't exist.** `Quote.status` values are stored as literal Thai strings (e.g. "รออนุมัติ") and the search predicate matches the raw stored value — an English-language searcher typing "pending approval" won't find it, even though the UI itself shows an English label via `statusLabelKey`/`t()`. Low Priority per the 2026-07-14 Codex review; not fixed this pass. Would need a small reverse lookup (English label text → Thai stored value) added to `searchQuotations()`'s `$or` in `api/_lib/searchHandler.ts`.
- [ ] **Customer search results don't show which field actually matched.** E.g. a search that matched on tax ID shows company/contact/phone/email but not the tax ID itself, so a user searching by tax ID can't visually confirm why that result appeared. Low Priority per the 2026-07-14 Codex review; not fixed this pass — would need either always showing tax ID (more data than necessary per-row) or a compact "matched by: taxId" indicator.
- [ ] **No automated test coverage for Global Search** (RBAC per category, regex escaping, archived/deleted exclusion, query-length bounds, debounce/stale-request handling, keyboard navigation). Flagged Medium by the 2026-07-14 Codex review. This project has no automated test infrastructure anywhere — a longstanding, separate, already-documented decision (see RBAC.md "Known Gaps") — not something a single feature's fix pass should introduce in isolation.
- [ ] **Global Search performs a full collection scan per category at real data volume.** Substring (not prefix-only) regex matching across a `$or` of several fields can't be efficiently served by a standard MongoDB index regardless of how many single-field indexes exist (`ensureSearchIndexes()` in `api/_lib/searchHandler.ts` still adds them — they help exact-match/sort use cases and keep the query planner's working set smaller, just not the substring match itself). Acceptable at this ERP's actual scale (one internal company, not big-data volume) — documented, not a defect. Revisit with MongoDB Atlas Search (`$search`) if data volume ever grows enough to matter; not introduced now since it isn't already configured and isn't clearly beneficial today.
- [x] ~~Normal page navigation (Quotations/Products/Customers/Users/Roles/Settings) still blocked behind unrelated boot data~~ — **done 2026-07-14** (independent Codex review of the progressive-loading pass, High #1): `App.tsx`'s single shared `initialDataLoading` flag replaced with per-resource `resourceStatus` tracking + a `NAV_RESOURCES` map naming which of the 9 boot resources each page actually needs, so e.g. navigating to Products no longer waits on unrelated `notifications`/`quotes`/`users` fetches. See CHANGELOG.md.
- [x] ~~Dashboard workflow-triggered refresh (approve/reject) left stale stats on screen with no refreshing indication~~ — **done 2026-07-14** (same review, High #2): `refreshAfterAction()` now also calls `setLoading(true)`, surfacing a small "กำลังอัปเดตข้อมูล..." indicator next to the page title while the previous data stays visible (new `dashboard.refreshing` i18n key). See CHANGELOG.md.
- [x] ~~A single optional Dashboard section failure (index creation, activity timeline, sales activity, approval dashboard, notifications) blocked the entire response~~ — **done 2026-07-14** (same review, High #3): each of the four sections is now isolated in its own try/catch inside `api/dashboard/index.ts`, degrading to `null`/a safe zero default instead of 500ing the whole response. KPIs/pipeline/salesPerformance/etc. now survive a failure in any one of them. See CHANGELOG.md.
- [x] ~~First-load Dashboard skeleton was shell-first but not section-first (one generic block, no real section titles/table headers)~~ — **done 2026-07-14** (same review, Medium): `DashboardContentSkeleton` rebuilt to mirror the real 4-section structure with actual translated titles (via `t()`) and, for `ActivityTimeline`, the real table header row — reusing `ChartCard` directly for pixel-identical header markup. See CHANGELOG.md.
- [x] ~~`computeQuoteAmountBeforeVat()`'s missing-`lines` fallback was undocumented and unmonitored~~ — **done 2026-07-14** (same review, Medium): `api/dashboard/index.ts` now emits a `console.warn` naming the affected count if any quote in the filtered set has no `lines` field at all; `docs/DATABASE.md`/`MODULES/Dashboard.md` corrected from "cannot occur" to documenting the actual (expected-to-be-null-set) fallback. See CHANGELOG.md.
- [x] ~~Stale documentation: `docs/MODULES/Customer.md`/`docs/TODO.md`/`docs/PROJECT_STATUS.md`/`docs/ARCHITECTURE.md` still described `/api/customers` as sharing the removed `company-profiles.ts` function, and had historical wording implying Company Profiles remains available~~ — **done 2026-07-14** (same review, Medium): all four corrected — see CHANGELOG.md for the itemized list.
- [x] ~~`customerSnapshot` is persisted but never displayed when reopening a quotation~~ — **done 2026-07-14** (Codex review of the Customer Management correction pass, High #1): `QuoteDocument.tsx` now seeds every Customer Information field's `useState` from `quote.customerSnapshot` first (via `??`, so a real stored empty string is respected), falling back to the quote's legacy top-level fields, then `""` — the specified (1) snapshot → (2) legacy field → (3) empty order. Deliberately does not add a further live customer-master lookup as a 4th tier (would overwrite user edits with today's master data on every reopen). `PrintDocument.tsx` needed no change — it renders the same now-correctly-seeded state. See CHANGELOG.md and [MODULES/Quotation.md](./MODULES/Quotation.md) "Customer Selection."
- [x] ~~Selecting a customer can leave stale optional delivery/project fields~~ — **done 2026-07-14** (same review, High #2): `handleSelectCustomer` in `QuoteDocument.tsx` previously only overwrote `deliveryMethod`/`project`/`deliveryAddress` when the selected customer's value was truthy, so a customer with blank delivery info left whatever the *previous* selection/manual entry had typed there — and that stale value then got saved into the new customer's `customerSnapshot`. Fixed by assigning all nine fields unconditionally, including blanks. See CHANGELOG.md.
- [x] ~~Dead issuer-branded types/naming and stale present-tense issuer documentation~~ — **done 2026-07-14** (same review, Medium): removed unused `IssuerCompanySnapshot`/`issuerDisplayFromProfile()`/`issuerDisplayFromSnapshot()` from `src/lib/companyProfiles.ts`; renamed the still-used single-company header type `IssuerCompanyDisplay` → `CompanyHeaderInfo`, moved to `src/lib/storage.ts`; corrected stale present-tense doc comments (`companyProfiles.ts`, `auditLog.ts`, `api/handlers/company-profiles.ts`) and doc sections (`docs/MODULES/CompanyProfiles.md`'s "Quotation Integration"/Audit Logging text, `docs/CLAUDE.md`'s Company Profiles table row, `docs/MODULES/Quotation.md`'s "Customer Selection" section) that read as presenting the removed issuer feature as still current. See CHANGELOG.md.
- [ ] **Selector has no server-side search, 20-result client-side cap** (Low Priority, Codex review 2026-07-14) — acceptable at today's real customer-list scale, matches the Company Profiles/Products precedent of client-side filtering over a fully-fetched list; revisit if the customer list grows large. Not fixed this pass, deliberately.
- [ ] **Selected-customer chip shows only the company name**, not contact name/tax ID (Low Priority, Codex review 2026-07-14) — would make selection confirmation clearer, not a functional gap. Not fixed this pass.
- [x] ~~Sales Activity Analytics was hidden from every Dashboard role without `auditLog:view`~~ — **done 2026-07-14** (second same-day pass, an independent Codex review's Critical finding): `api/dashboard/index.ts`'s `salesActivity` no longer requires `auditLog:view` — only the route's own `dashboard:view` gate, same as every other required-section field. Default Sales User/Approver 1/Approver 2/Viewer roles (all `dashboard:view`, none `auditLog:view`) now see the required "กิจกรรมของฝ่ายขาย" section. `activityTimeline` (the raw audit-log feed) intentionally stays `auditLog:view`-gated. See CHANGELOG.md.
- [x] ~~Sales Activity Analytics ignored the date-range filter's start date~~ — **done 2026-07-14** (second same-day pass, Codex High Priority finding): `activityMatch.createdAt` now bounds by `bangkokDayBoundsUtc(from, to)` when set, same pattern as `activityTimeline`. `SalesActivityAnalytics.tsx`'s caption now switches between "rolling trend" and "filtered to selected range" text depending on whether `stats.filters.from` is set, so the UI never claims a behavior the query isn't doing. See CHANGELOG.md.
- [x] ~~An empty database hid the required 4 KPI cards behind one full-page empty state~~ — **done 2026-07-14** (second same-day pass, Codex High Priority finding): `DashboardPage.tsx`'s page-wide `!hasAnyData` gate removed; every required section now always renders (each already has its own per-widget empty-state fallback), with a compact inline banner instead of a full-page block when the database is genuinely empty. See CHANGELOG.md.
- [ ] **No soft-delete field exists on `Quote` — Dashboard quote queries have no `isDeleted` exclusion, by design for now.** Flagged High Priority by the 2026-07-14 Codex review as a forward-looking risk (not a demonstrated bug — confirmed `Quote`/`QuoteFields` has no such field anywhere today). Documented instead of code-filtered (see `api/dashboard/index.ts`'s comment above `dateMatch`/`fullMatch`) since filtering on a nonexistent field would be dead code. If quotation soft-delete is ever introduced, every quote query in `api/dashboard/index.ts` (`fullMatch`, `salespersonOnlyMatch`, and the won-revenue/follow-up/client-count/distinct-salesperson queries) must be updated together — a real schema/business-requirement decision, not a quick Dashboard-only fix.
- [x] ~~Dashboard pre-tax amount is backed out by dividing the VAT-included grand total, not computed from an authoritative source~~ — **done 2026-07-14** (Company Profiles removal / Dashboard rework pass): replaced `preTaxAmount(amount) = amount / 1.07` with `computeQuoteAmountBeforeVat(lines, discount)` (new shared `api/_lib/quoteAmounts.ts`), recomputed directly from each quote's own line items — the same inputs `quoteValidation.ts` uses to derive the persisted `amount`, never a division of it. `api/dashboard/index.ts`'s 3 raw-amount queries now project `lines`/`discount` instead. See CHANGELOG.md.
- [ ] **Spot-check the Dashboard's (now items-based) pre-tax amount calculation against real MongoDB data.** `computeQuoteAmountBeforeVat()` (`api/_lib/quoteAmounts.ts`) is verified by code inspection and `tsc`/`lint`/`build` (all clean) — a real end-to-end spot-check (pick one live quote, compute the expected value by hand from its `lines`/`discount`, confirm the Dashboard's reported figure matches) hasn't been run against production data — same sandboxed-session no-live-database limitation as every prior pass (see PROJECT_STATUS.md Known Risks).
- [ ] **Manually verify the 2026-07-14 Codex-fix-pass progressive-loading behavior against a live deployment.** Verified this pass via `tsc`/`lint`/`build` (all clean) and code-level review only — same sandboxed-session limitation. Specifically unverified against real network conditions: navigating to Products/Users/etc. while other boot resources are still slow-loading actually shows that page's own shell promptly; the Dashboard refresh indicator actually appears (and disappears) correctly around a real Approve/Reject action; a real forced auditLog-query failure actually degrades to a hidden section rather than an error page.
- [x] ~~Sales Activity Analytics only tracks 2 of the 5 requested event categories~~ — **done 2026-07-13** (sixth same-day pass, flagged by an independent Codex review as High Priority): `api/dashboard/index.ts`'s `salesActivity` aggregation now buckets by `categoryForAction()` into all 5 (Created/Edited/Status Changed/Approval Requested/Approval Completed), rendered as a stacked bar chart in `SalesActivityAnalytics.tsx`. See CHANGELOG.md.
- [x] ~~Dashboard "Recent Activities" has no structured "related quotation/customer" field~~ — **done 2026-07-13** (seventh same-day pass, part of the P'Keng/P'Kee business-requirement completion pass): `AuditLogEntry` gained optional `relatedQuoteId`/`relatedCustomerName` fields, written by `writeQuoteAuditEntry()` on every quote-workflow entry; `ActivityTimeline.tsx` rebuilt as a table with a clickable quotation-number link. Backward-compatible — older/non-quote entries render "—". See CHANGELOG.md.
- [x] ~~Company Profiles: boot sequence broke for any role without `companyProfiles:view`~~ — **done 2026-07-13** (tenth same-day pass, found during an independent Codex review): `fetchCompanyProfiles()` was unconditional in `App.tsx`'s shared boot/sign-in `Promise.all`, so a 403 for Sales/Approver/Viewer roles rejected the whole fetch and left `bootStatus` stuck at `"loading"` forever. Fixed with `.catch(() => [])` at all 3 call sites; also fixed `handleSignIn` never having fetched company profiles at all (separate gap from the same pass). See CHANGELOG.md.
- [x] ~~Company Profiles: default-profile invariants unsafe (deactivatable, concurrency race, inactive-first-default)~~ — **done 2026-07-13** (tenth same-day pass, Codex High #1/#2 + a Medium finding): PATCH now blocks deactivating the current default; a partial unique MongoDB index (`{isDefault:1}` where `isDefault:true`) now guarantees at most one default at the database level, with both create and set-default catching the resulting duplicate-key race and responding cleanly; the auto-assigned first profile is now always forced active. See CHANGELOG.md.
- [x] ~~Company Profiles: audit entries were client-authored, not server-authoritative~~ — **done 2026-07-13** (tenth same-day pass, Codex High #3): `writeCompanyProfileAuditEntry()` now writes every Company Profile audit entry inside `api/handlers/company-profiles.ts` itself, with structured `relatedCompanyProfileId`/`relatedCompanyProfileName` linkage; `POST /api/audit-log` now rejects the `"โปรไฟล์บริษัท"` module, mirroring the quotation lockout. See CHANGELOG.md.
- [x] ~~Company Profiles: no live-database concurrency test for the partial unique `isDefault` index~~ / ~~`GET /api/company-profiles` has no pagination, server-side filtering, or list projection~~ — **both moot as of 2026-07-14**: the entire Company Profiles module (not just its Quotation integration) was removed from the user-facing ERP — see below and [MODULES/CompanyProfiles.md](./MODULES/CompanyProfiles.md).
- [x] ~~Wire Company Profiles into the Quotation module~~ — **done 2026-07-13** (eleventh same-day pass), **then fully reverted 2026-07-14**: this entire "issuer company" integration was built against a misunderstanding of the actual requirement (this ERP only ever has one issuer company) — see the correction item below. Kept here struck through only as a historical pointer.
- [x] ~~**Correction: remove the incorrect Quotation "Issuer Company" feature; build the correct Customer selection feature**~~ — **done 2026-07-14**: `IssuerCompanySelector.tsx` deleted, `resolveIssuerCompanyUpdate()` and every issuer branch removed from `api/handlers/quotes.ts`, `Quote.issuerCompanyId`/`issuerCompanySnapshot` removed from the client type. Replaced with `CustomerSelector.tsx` + `Quote.customerId`/`customerSnapshot`, a redefined `customers` collection, a `CustomersPage.tsx` admin page, 4 new `customers:*` permissions, and `resolveCustomerIdUpdate()`/`buildCustomerSnapshot()` in `api/handlers/quotes.ts`. The Company Profiles module itself (admin CRUD) was, at the time of this pass, untouched and still available — just no longer read by Quotation. **Superseded later the same day (2026-07-14) — see item below "Remove the Company Profiles module from the user-facing ERP": the module itself was subsequently removed too**, on explicit instruction, since a page for managing multiple issuer companies was unused scope once this ERP was confirmed to only ever need one. This makes every open TODO item below about the old issuer-company feature **moot** (the feature they described no longer exists), kept struck-through for history rather than deleted outright. See CHANGELOG.md, [MODULES/Customer.md](./MODULES/Customer.md), [MODULES/CompanyProfiles.md](./MODULES/CompanyProfiles.md) "Removed (2026-07-14)."
- [x] ~~Company profile `quotationPrefix`/`quotationNumberFormat` still don't compose with quotation numbering~~ — **moot as of 2026-07-14** (see correction item above; there's no issuer-company concept left to compose numbering against).
- [x] ~~No hard block on saving a quotation Draft with no issuer company selected~~ — **moot as of 2026-07-14**, same reason.
- [x] ~~`handleWorkflow` doesn't write a distinct "Quotation Issuer Company Changed" audit entry~~ — **moot as of 2026-07-14**, same reason (the workflow route now writes `"Quotation Customer Changed"` instead, when applicable).
- [x] ~~Issuer display fallback chain skips the default active profile for a stale/partial-data quote~~ — **done 2026-07-13** (twelfth same-day pass, Codex review Medium #1), **then moot as of 2026-07-14** once the whole issuer-display feature was removed.
- [x] ~~Codex review (twelfth pass) Medium/Low items deliberately left unfixed~~ — **moot as of 2026-07-14**, all concerned the now-removed issuer-company feature.
- [x] ~~No live-database concurrency/manual-verification pass yet for the 2026-07-13 Quotation-issuer integration~~ — **moot as of 2026-07-14**. The equivalent open item for the replacement Customer feature: no live-database browser verification yet (same sandboxed-session limitation, see Known Risks below) — verified instead via `tsc -b`/`tsc --noEmit -p tsconfig.api.json`/`npm run lint`/`npm run build` (all clean). A real manual walkthrough (create a Customer, select it on a quote, confirm autofill + edit-after-autofill + Draft-only lock + snapshot preserved on reopen) still needs running against a real deployment.
- [ ] **Old quotations may carry stray `issuerCompanyId`/`issuerCompanySnapshot` fields in MongoDB** from the brief 2026-07-13–2026-07-14 window when that feature existed — harmless (no code reads them anymore), not backfilled/cleaned up since there's no functional reason to touch old documents. A future data-hygiene pass could strip them, not urgent.
- [ ] **"บันทึกเป็นลูกค้าใหม่" (save a manually-typed Quotation Customer Information section as a new Customer record, inline from the form)** — explicitly flagged as an optional future nice-to-have in the 2026-07-14 requirement ("do not add unless simple and safe"), not built this pass. A Sales user creates Customers via the dedicated admin page today.
- [ ] **Vercel Hobby's 12-function cap is still fully used.** `api/handlers/company-profiles.ts` (2026-07-13) was the 12th and final slot; it was deleted 2026-07-14 along with the rest of the Company Profiles module, but the freed slot was immediately spent giving `api/handlers/customers.ts` its own dedicated file again (previously folded into `company-profiles.ts`) — net function count unchanged, still 12 of 12. Any future new API resource must be added as a new dispatch branch inside an existing handler file rather than a new file — or the project needs to move off the Hobby plan first. See [ARCHITECTURE.md](./ARCHITECTURE.md)/[API.md](./API.md).
- [x] ~~Remove the Company Profiles module from the user-facing ERP~~ — **done 2026-07-14**: admin UI (`src/pages/admin/companyProfiles/`), client lib (`src/lib/companyProfiles.ts`), API (`api/handlers/company-profiles.ts`, `api/_lib/companyProfileValidation.ts`), `vercel.json` rewrites, `companyProfiles:*` permissions (union/labels/groups/default-role grants), and ~110 i18n keys all removed. `company_profiles` MongoDB collection and any existing documents/audit-log entries deliberately left untouched (no destructive DB cleanup) — see [MODULES/CompanyProfiles.md](./MODULES/CompanyProfiles.md) and CHANGELOG.md.
- [ ] **Medium/Low Priority items deferred from the 2026-07-13 sixth-pass Codex review** (data/filter-correctness review, `docs/CODEX_REVIEW_REPORT.md` — distinct from the earlier UI/UX review bucketed below) — the Critical (none found) + High items were fixed the same day (see CHANGELOG.md):
  - Overdue Follow-ups/Expired Quotations task tiles (`ActivityFollowUpSummary.tsx`) still aren't clickable — no matching date-derived quotation-list filter exists to link to yet.
  - Supporting-detail area (below the "In-Depth Detail" divider) still requires substantial scrolling — no collapse/expand control.
  - Charts without an equivalent full data table (forecast, revenue trend) — Sales Activity and Quotation Status already double as a table.
  - Repeated inline Playfair Display/color `style={{...}}` objects across Dashboard components — not consolidated into shared typography/color tokens.
- [ ] **Medium/Low Priority UI items deferred from the 2026-07-13 Codex review** (`docs/CODEX_REVIEW_REPORT.md`) — the Critical + High items were fixed the same day (see CHANGELOG.md), these were explicitly scoped out:
  - Topbar search is decorative (no state/filtering/results) — either wire it up or remove the input to stop implying it works.
  - ~~Notification delete button is hover-only (`opacity-0 group-hover:opacity-100`) — no persistent affordance for touch/keyboard users.~~ — **done 2026-07-29** (UX pass): always visible at 50% opacity, full opacity on hover/focus-visible.
  - Notification rows are `<div onClick>`, not semantic `<button>`/`<a>` — no keyboard activation.
  - ~~Login page's "Remember me" checkbox is UI-only and doesn't change session behavior — make it functional or remove it (currently misleading).~~ — **done 2026-07-29** (UX pass): removed (sessions are always the 7-day cookie; the checkbox only misled).
  - No `focus-visible` ring audit across icon-only buttons (many rely on `title` alone, not an accessible name for all assistive tech).
  - Chart cards use fixed pixel heights (200–260px) not re-tuned per breakpoint; Dashboard tables/legends have no compact-card mobile alternative, only horizontal scroll.
- [ ] **Migrate the 11 older tour-replay buttons to `TourReplayButton`.** The shared component was extracted into `src/components/GuidedTour.tsx` during the 2026-07-29 review pass (the markup had been copy-pasted 14 times and drifted into two size variants: `w-9 h-9`/`size 15` on the 10 list/admin pages vs `w-8 h-8`/`size 14` on DashboardPage + the three document editors, which now use the component). Mechanical, per-file swap; decide on the one canonical size while at it (also the natural moment to add the focus-visible ring the a11y item above calls out).
- [ ] **Roll out `PageHeader`/`EmptyState` beyond the Dashboard.** Added 2026-07-10 (UI/UX pass) as shared components (`src/components/PageHeader.tsx`/`EmptyState.tsx`) and wired into the Dashboard (both) plus Products/Quotations (`EmptyState` only); Quotation/Products/Settings/User Management/Role Management/Audit Log pages still use their own hand-rolled `<h1>`/description header markup. Mechanical, page-by-page work — low risk, no new design decisions needed, just needs the time.
- [x] ~~**Extend the guided tour to other modules**~~ — **done 2026-07-29, every existing page covered** (owner: "ทำให้หมด"): `useModuleTour()` (GuidedTour.tsx) gives each page its own driver.js walkthrough with per-user/per-tour "seen" tracking (`hasPageTourCompleted()`, src/lib/tour.ts), auto-started once per user on first visit and replayable via a HelpCircle button in each page's header. Live tours (11 + the original main tour): QuoteList, ProductList, ScopeOfWorkList (incl. the "เฉพาะที่ยังไม่มี PO" toggle step), DeliveryOrderList, CustomersPage, TemplateManagementPage, UserManagementPage, RoleManagementPage, AuditLogPage, SettingsPage (tabs + profile/signature step), and DashboardPage (5 deeper steps: export/filters/KPIs/status/in-depth; **auto-starts only after the MAIN first-sign-in tour is completed/skipped** via `useModuleTour`'s new `autoStart` option, so two driver.js instances never race on a brand-new user's first visit). **Document-editor tours added later the same day** (the final batch): QuoteDocument (actions/customer selector/line items), ScopeOfWorkDocument (actions/completion indicator/header incl. manual-number + PO rules/checklist incl. recipients), DeliveryOrderDocument (actions/per-installment cards). Auto-fire gating (corrected by the same-day review pass): ScopeOfWorkDocument passes `autoStart: !!scope` (waits for the fetch), DeliveryOrderDocument passes `autoStart: !!deliveryOrder && installments.length > 0` (waits for the fetch AND for the per-installment cards the step describes — an SOW with an empty/deposit-only payment schedule yields `installments: []`), and QuoteDocument passes `autoStart: isDetail` (its record arrives via props — the race there was create-vs-detail MODE, not loading: the actions step describes `isDetail`-gated buttons). **The only remaining gap is Leads** — no page exists to point at; add its tour when the module gets built, not before.
- [ ] **In-app unsaved-changes interception for the Quotation form.** The 2026-07-10 UX pass added a `beforeunload` browser warning (tab close/refresh/browser back), but this app has no router — switching sidebar sections mid-edit is a plain React state change, not a browser navigation event, so it isn't currently intercepted. Would need `QuotationPage`'s `onBack`/`App.tsx`'s `setActiveNav` calls to check a "is the form dirty" flag and show a `ConfirmDialog` before discarding, threaded up from `QuoteDocument.tsx`.
- [ ] **Accessibility audit.** The 2026-07-10 UX pass added basic `aria-label`/`aria-expanded`/focus-visible handling to the new components only (`MetricInfoTooltip`, `GuidedTour`'s driver.js defaults); no audit was done of the rest of the app (icon-only buttons without labels, color-only status indicators, keyboard-nav gaps, contrast). A real pass would need to go screen-by-screen.
- [ ] **[Business decision needed]** `GET /api/users` returns every field but `passwordHash` (phone/email/department/position/profile picture/signature image) to any authenticated user — flagged Medium by the 2026-07-10 Codex review. Not changed: the app relies on the full user directory in multiple places that would break under a naive field-strip (printed-quote preparer/approver signature images visible to any `quotations:view` holder, salesperson pickers, avatar pictures in lists), and this is a single-company internal tool where staff-directory visibility is a defensible default, not obviously wrong. **Decide**: (a) accept the current model explicitly (any signed-in employee, no PII secrets/passwords exposed), or (b) split into a `users:manage`-gated "full profile" response vs. a public `{id, fullName, department, roleKey, signatureDataUrl}`-only response for everyone else, tracing every current consumer of the wider field set first. See `api/handlers/users.ts`'s comment for the full tradeoff.
- [ ] **[Business decision needed]** Department/salesperson Dashboard filtering is a free-text join (`User.department` ↔ `Quote.salesperson` matched by exact name string) — accurate only as long as names never collide, get typo'd, or get renamed after the fact. Reconfirmed as a real, still-open High Priority finding by an independent 2026-07-13 (eighth same-day pass) Codex review — deliberately not fixed that pass since it needs this business decision first, not a quick code change; see CHANGELOG.md. A real fix needs `Quote` to store a `salespersonUserId` reference instead of (or alongside) the free-text `salesperson` display string, which itself requires deciding: should the Quotation form's Salesperson field stop being freely editable text and become a locked dropdown of real `User` records? That's a real UX/workflow change (today a preparer can type any name, e.g. crediting a colleague who didn't log in themselves) that needs product sign-off, not a guess. See `api/dashboard/index.ts`'s department-join comment and [MODULES/Dashboard.md](./MODULES/Dashboard.md) caveats.
- [ ] Add real `createdAt`/`updatedAt` timestamp fields to `Quote` (currently has neither — only `issueDate`/`date` business-date strings and `createdByUserId`/`updatedBy` user-id references; found while correcting a stale DATABASE.md claim during the 2026-07-10 Codex review response). Would improve auditability and enable real time-range reporting beyond `issueDate`-based filtering. Needs a migration decision (backfill existing quotes from `approvalHistory[0].createdAt` where available, or leave historical quotes with `createdAt: null`).
- [x] ~~`App.tsx`'s session-fetch boot effect has no error handling~~ — **done 2026-07-14** (progressive-loading pass): the boot `useEffect`'s `fetchSession()` call is now wrapped in `try`/`catch`; a thrown network/API error sets a new `bootError` state and renders a retryable `BootError` screen instead of leaving `bootStatus` stuck at `"loading"` forever. Same pass also removed the further blocking wait on the 9-way domain-data `Promise.all` — the sidebar/header shell now renders as soon as the session check itself resolves, with each domain fetch updating its own state independently. See CHANGELOG.md.
- [ ] **Verify both 2026-07-10 Dashboard passes against live data.** `tsc`/`lint`/`build` pass clean each time and the feature went through a full 10-angle code review (10 real bugs found/fixed) plus a second, spec-completion pass (12 more gaps found/fixed — see CHANGELOG), but no browser pass against real MongoDB data has happened yet. The second pass's session **did** have local DB credentials (`.vercel/.env.development.local`) and got `vercel dev` running locally with the real API, but every MongoDB-touching route — including the pre-existing, untouched `GET /api/auth/session` — failed with `querySrv ECONNREFUSED _mongodb._tcp.tcsdb.zdnus3w.mongodb.net`: that sandboxed session's Node process can't resolve MongoDB Atlas's SRV DNS record, even though the OS resolver and raw TCP both work. Needs either a different network environment for local `vercel dev`, or a Vercel preview-deployment click-through, or explicit sign-off to test against production with cleanup afterward. See [PROJECT_STATUS.md](./PROJECT_STATUS.md) "Known Risks."
- [ ] **Pre-existing timezone bug in `src/lib/quotes.tsx`'s `todayIso()`** (discovered, not introduced, by the 2026-07-10 code review) — uses the same `new Date().toISOString().slice(0,10)` pattern found broken in the Dashboard's date math, used as the default `issueDate` for a brand-new quote. A user creating a quote between local midnight and 7am Thailand time could get "yesterday's" date as the default issue date. Not fixed as part of the Dashboard review since it's pre-existing code outside that diff's scope — needs its own scoped fix + verification.
- [ ] **Cleanup items found but deliberately deferred during the 2026-07-10 Dashboard code review** (see CHANGELOG for the full list): a repeated percentage-rounding formula in `api/dashboard/index.ts` that could be a shared `pct()` helper; `salesPerformance`/`customerAnalytics`/`jobTypeAnalytics`'s O(n·k) grouping pattern (fine at current data volume, revisit if it grows); `SalesPerformanceTable.tsx`/`JobTypeAnalytics.tsx` hand-rolling card markup instead of the already-extracted `ChartCard`; a "no data" empty-state message duplicated across 5 dashboard files instead of sharing one component; `PipelineFunnel.tsx` maintaining its own color palette instead of reusing `DashboardCharts.tsx`'s; `SalesPerformanceTable.tsx`'s column-sort UX not matching `ProductList.tsx`'s existing toggle-direction pattern; `escapeRegExp()` duplicated a 4th time in `api/handlers/jobtypes.ts` (already duplicated 3× pre-existing in `auth.ts`/`categories.ts`/`roles.ts`).
- [ ] **Rotate the MongoDB Atlas database-user password.** A real credential was pasted into an AI chat session during the 2026-07-09 backend migration's development — a rotation was recommended to the user as a follow-up, but whether it's actually been done can't be verified from the codebase. Treat as unresolved until explicitly confirmed. See [RBAC.md](./RBAC.md) Known Gaps and [PROJECT_STATUS.md](./PROJECT_STATUS.md) Known Risks.
- [x] ~~**Set up CI** (typecheck + lint + build on push/PR)~~ — **done 2026-07-29**: `.github/workflows/ci.yml` (GitHub Actions, platform-neutral per the no-Vercel-locked-services rule) runs `npm run lint` + `tsc --noEmit` (both tsconfigs) + `npm run build` on every push/PR to `master`, Node 24, `npm ci` with npm cache. **Notify-only, deliberately**: it does not block the Vercel auto-deploy (that would require moving to a PR-based workflow — a real workflow change, only worth doing if the owner wants it); a broken push now shows a red ✗ on the commit immediately so it can be reverted fast. See CHANGELOG.md.
- [x] ~~**Verify GitHub → Vercel auto-deploy is actually wired.**~~ — **confirmed in practice by 2026-07-23**: the `RESEND_API_KEY` pass observed a real push produce a `READY`/`production` deployment matching the latest commit via the Vercel MCP tools, and every user-visible feature shipped since has reached production the same way. No longer an open question.
- [x] ~~**Add rate limiting to `POST /api/auth/login`**~~ — **done 2026-07-29**: failed attempts are recorded in a new `login_attempts` MongoDB collection (TTL-purged after 15 min; portable, per the no-Vercel-locked-services rule — no per-instance memory, no KV service) and counted over a 15-minute sliding window, keyed per typed identifier (≥5 failures → 429) and per requesting IP (≥20 → 429, higher so one office NAT with several fat-fingering humans doesn't trip it). The 429 carries a Thai "รอประมาณ X นาที" message + `Retry-After`; a successful login clears that identifier's failures; a correct-password-but-suspended attempt neither records nor clears. The check runs before the bcrypt compare, so locked-out requests stay cheap. Indexes declared both in `ensureIndexes()` and defensively at runtime (`ensureLoginAttemptIndexes()`, same pattern as `ensureScopeNumberIndexes()`). **Unverified live** (same standing limitation): that 6 rapid wrong passwords really 429 on production, and that the lockout expires on schedule. See CHANGELOG.md.
- [ ] **Add automated tests, especially for the new API layer** (`api/handlers/*.ts`) — **first real suite landed 2026-07-29** (55 vitest tests, `tests/`, `npm test`, wired into CI with an in-memory MongoDB for integration tests): quotation money math incl. client/server formula parity, default-role RBAC grants + permission edge cases, the quotation `workflowTransitions` state machine + `isWorkflowActionAllowed` per-action rules, `computeQuotePermissions` ownership rules, revision-chain parsing/dedup, Scope of Work required-field validation (incl. the 2026-07-29 manual-number rules), and a full login integration test (real bcrypt/JWT + the new rate limiting, 429/clear-on-success/IP-cap/suspended-account/TTL-index cases). **Still open**: end-to-end route coverage beyond `/api/auth/*` — the per-route permission-gating/ownership guards in `api/handlers/{users,roles,quotes,products,...}.ts` (incl. the last-active-Super-Admin guards) are covered only at the pure-logic level, not as HTTP-level integration tests per route. The `tests/api/loginRateLimit.test.ts` harness (mock Vercel req/res + mongodb-memory-server) is the template to extend.
- [x] ~~Customer Management module — API routes + UI~~ — **done 2026-07-14**: `customers` collection redefined (see [Customer.md](./MODULES/Customer.md) "Field shape change"), `GET/POST /api/customers`/`GET/PATCH /api/customers/:id`/`POST /api/customers/:id/archive` (sharing the `company-profiles` function), `CustomersPage.tsx` admin CRUD, `customers:*` permissions, and Quotation-form `CustomerSelector` integration. `customer_contacts` remains schema-only (not needed — one contact per customer is what the form uses).
- [ ] **Lead Management module — API routes + UI** — original Phase-1 scope, still untouched. **Schema-level prep done 2026-07-09**: `leads`/`lead_activities` MongoDB collections + indexes exist (`api/_lib/collections.ts`), and the Dashboard's `totalLeads` KPI already queries them (correctly `0` today). What's left: API routes (`api/handlers/leads.ts` following the established pattern) + UI (`src/lib/leads.ts`+`src/pages/leads/`, mirroring `products.ts`/`ProductsPage.tsx` or the new `customers.ts`/`CustomersPage.tsx`).
- [ ] Wire `Company.vatRate` (Settings → Company Info) into `computeTotals()` in `lib/quotes.tsx` — currently stored/editable but the actual 7% VAT calculation still uses the hardcoded `VAT_RATE` constant.
- [ ] Add button-level (create/edit/delete) permission gating to Product Library — currently only the sidebar entry respects `products:view`; the create/edit/delete buttons inside `ProductsPage`/`ProductList` aren't gated by `products:create`/`products:edit`/`products:delete`. (Note: this is a UX-only gap — the underlying `POST`/`PATCH`/`DELETE /api/products` routes are already properly permission-gated server-side.)
- [x] ~~Lift `QuotationPage`'s `view`/`selectedId` state up to `App.tsx` so a notification click can deep-link straight to the specific quote's detail view~~ — **done 2026-07-10** (Codex review fix pass): a `quotationDeepLinkId` prop (separate from the pre-existing `quotationListFilter`) is set by the notification click handler and consumed by `QuotationPage` via a `useEffect` that reacts to every change (not just mount), so it works whether the module is mounting fresh or already open. See [MODULES/Quotation.md](./MODULES/Quotation.md).
- [ ] Consider raising bcrypt's cost factor from 10 (the `bcryptjs` default, not explicitly tuned during migration) if login latency budget allows — the originally-proposed design called for 12.
- [x] ~~Add explicit MongoDB indexes~~ — **done 2026-07-09**: real indexes now exist across every collection, see [DATABASE.md](./DATABASE.md).

## Medium Priority — PDF / Quotation document polish (2026-07-08 request)

- [x] Company **logo** upload (Settings → Company Info) + render in the quotation PDF header, replacing the text-only "TCS ERP" wordmark when set
- [x] Company **stamp** upload (optional), rendered near the "ผู้อนุมัติ" signature block when set
- [x] **Prepared By**: "พนักงานขาย" is now a real controlled field, bound to `Quote.salesperson`, defaulting to the signed-in user's name on new quotes; the "ผู้เสนอราคา" signature line pre-fills that name in print instead of dots. (Actual signature **image** upload is still pending — see User Profile below; this only covers the name.)
- [x] **Hide empty fields automatically** in the PDF/print view — verified: a fresh quote with no contact/phone/address/tax ID/PO filled in prints none of those rows
- [x] **Do not display placeholder text** in the PDF — `ผู้ติดต่อ`/`เบอร์โทร`/`ที่อยู่`/`เลขประจำตัวผู้เสียภาษี`/`อ้างอิง PO`/`เงื่อนไขการชำระเงิน`/`วันที่ออกเอกสาร`/`วันหมดอายุ` are now real controlled fields on `Quote`, saved per-quote, not hardcoded example values
- [x] Quotation item **tags** — chip input in the line-item expand panel, rendered as pills on screen and in print
- [x] Quotation item **specifications** field (distinct from notes) — added to `QuoteLine`; copied from `Product.specifications` automatically when a line is added via the product picker
- [ ] "Nested" sub-details — **decision made**: keeping the flat reorderable list. Re-open only if a concrete business case for sub-sub-details shows up; not building speculative tree UI for it now.

## Medium Priority — Print/PDF redesign (2026-07-09 request)

- [x] Repeating-header print document matching a real vendor quotation reference (`src/pages/quotation/PrintDocument.tsx`) — company/buyer/meta header + column headers repeat every page via `<thead>`, verified with a forced multi-page export
- [x] Per-unit discount (amount + %) column on printed line items
- [x] Pin-icon sub-detail bullets in print
- [x] Thai-words amount line under the grand total (`bahtText()`)
- [x] Three-column signature table (adds a blank "ผู้ยืนยันการสั่งซื้อ" customer-confirmation column)
- [x] Buyer contact email, delivery method, delivery address, project fields
- [x] Fixed: remarks/terms textarea was uncontrolled and never persisted — now a real `Quote.remarks` field
- [ ] "Page X/Y" numbering — not implemented; no reliable cross-browser way to read total page count from CSS in a browser print/PDF context
- [ ] Condensed header on continuation pages (page 2+) — not implemented; a browser print `<thead>` can't vary content by page number, only revisit if this becomes a real requirement (would need a JS-driven pagination approach, a bigger change)

## Medium Priority — User Profile (2026-07-08 request)

- [x] Profile picture upload — `ImageUploadField` in Settings → Profile, saved on `User.profilePictureDataUrl`
- [x] Signature **image** upload, auto-used in quotation PDFs — `User.signatureDataUrl`, rendered on the quote's preparer/approver signature block by looking up the creator and the most recent approval-history entry; falls back to a blank line if unset, never an error

## Medium Priority — Company Settings expansion (2026-07-08 request)

- [x] Company logo + stamp management — done (see PDF polish above)
- [x] Bank account info fields — `Company.bankName`/`bankAccountName`/`bankAccountNumber`/`bankBranch`, Super Admin only
- [x] VAT rate as a configurable setting — `Company.vatRate` field exists and is editable, **but not yet read by the actual calculation** (see High Priority above — `computeTotals()` still uses the fixed `VAT_RATE` constant)
- [x] Terms & Conditions as an editable company-level default — `Company.termsAndConditions`, used as the default value of the quotation remarks textarea when set

## Medium Priority — RBAC / User Management / Approval Workflow / Notifications (2026-07-08 request)

- [x] Initial Setup Wizard (first-run only, creates the Super Admin, never reappears once any user exists) — `src/pages/SetupWizardPage.tsx`
- [x] Multi-user accounts with hashed passwords, Employee ID/Department/Position/Role, active/inactive status — `src/lib/users.ts`; public self-signup removed
- [x] RBAC: 6 default roles, 17-permission model, fully-hidden (not disabled) permission-gated sidebar — `src/lib/{roles,permissions}.ts`
- [x] User Management page (create/edit/reset password/activate/deactivate/assign role-department-position) — `src/pages/admin/UserManagementPage.tsx`
- [x] Role Management page (Super-Admin-only hardcoded, create/delete custom roles, permission matrix editor, system roles read-only) — `src/pages/admin/RoleManagementPage.tsx`
- [x] Quotation approval workflow (Draft → Pending Approval → Approved → Sent to Customer → Customer Accepted/Rejected → Won/Lost, + Cancelled), append-only approval history — `src/lib/quotes.tsx`, `src/pages/quotation/QuoteDocument.tsx`
- [x] Notification bell (correct 0/badge/99+ behavior) + panel + role-based delivery for quotation events — `src/components/NotificationBell.tsx`, `src/lib/notifications.ts`
- [x] Append-only, read-only audit log — `src/lib/auditLog.ts`, `src/pages/admin/AuditLogPage.tsx`
- [ ] Sequential two-level approval (Approver Level 1 must approve before Level 2 can) — **not implemented**, both approver roles currently have independent approve/reject rights from "Pending Approval". Only revisit if a concrete business need surfaces.

## Medium Priority — Other

- [ ] **"Level 2" page persistence: restore the OPEN DOCUMENT (not just the page) across a
  refresh.** The 2026-07-29 URL-hash persistence keeps the top-level page (`#quotations`, ...)
  across refresh/Back/Forward, per the owner's request — but which document a page had open
  (a specific quotation/SOW/DO in detail view) is each page's internal state and still resets.
  Doing this properly means encoding the open record id (and view mode) into the hash per module
  and restoring it on mount — start with the Quotation module if requested (owner was told this
  is a bigger, per-module job; approved only "level 1" so far). Unsaved form input can never
  survive a refresh regardless — set that expectation if this gets built.

- [x] ~~**Manual-ONLY Scope of Work document number entry (2026-07-24, owner: "ระบบไม่ต้องสร้างเลข
  เองดิ" — superseding the earlier optional-field framing the same day; recorded, not built).**~~ —
  **done 2026-07-29, exactly per the recorded spec**: user types the number at creation (modal in
  `QuoteDocument.tsx`) and on Duplicate (prompt in `ScopeOfWorkDocument.tsx`); server enforces
  non-blank + uniqueness (friendly 409 + unique-index race-safe backstop); editable while Draft
  only; Rewrite still appends `-R{n}`; `yearMonth`/`jobSequence`/`secondaryCode` became legacy
  (old records untouched, new ones write ""/0/""); Global Search/lists/email subject verified (by
  code reading) to follow for free; email threading unaffected. The build-time open question was
  answered by the owner: **completely free text, no format guardrails**. Discovered en route: the
  unique `scopeNumber` index TODO assumed existed almost certainly never got created on production
  (`ensureIndexes()` only runs from the Setup Wizard) — a new `ensureScopeNumberIndexes()` now
  defensively creates it and drops the legacy `{yearMonth, jobSequence}` unique index at runtime.
  See CHANGELOG.md 2026-07-29.
- [ ] **Manually verify the manual-ONLY Scope of Work number entry (2026-07-29) against a live
  deployment.** Verified via `tsc`/`lint`/`build` (all clean) and code review only — same standing
  sandboxed-session limitation as every recent pass. Specifically unverified against real data:
  creating with a typed number works end-to-end and a duplicate number really gets the friendly
  409 toast in the modal (not a raw error); `ensureScopeNumberIndexes()` actually creates the
  unique `scopeNumber` index on production (would fail loudly in logs if duplicate numbers somehow
  already exist) and drops `yearMonth_1_jobSequence_1` where present; editing the number on a
  Draft saves + re-checks uniqueness; a Final record's number field is genuinely read-only;
  Duplicate's prompt → new number → new record flow; Rewrite of a manually-numbered record
  produces `{typed}-R1`; old auto-numbered records still list/search/open normally.
- [x] ~~**"ทวง PO" (chase the sales for the customer PO number + PO file) — proposed 2026-07-24,
  awaiting the owner's go-ahead.**~~ — **done 2026-07-29, full proposal built on the owner's
  direct go-ahead ("ทำเรื่องทวง PO ต่อเลย")**: (1) "ยังไม่มี PO" badge + new PO column, an
  "เฉพาะที่ยังไม่มี PO" filter toggle, and a 5th summary card on the Scope of Work list (badge
  basis: blank `customerPoNumber` — attachments deliberately not consulted, they carry no type to
  tell a PO file apart); (2) "ทวงเลข PO" toolbar button on the record (shown while the PO number
  is blank) → `POST /:id/chase-po` sends an in-app bell notification (new
  `scope_of_work_po_chase` type, deep-linked via `relatedScopeId`) to the resolved salesperson
  (name-matched user → `seller.userId` → creator), audit-logged and repeatable; (3) Dashboard's
  Scope of Work card gained a "ยังไม่มีเลข PO" counter. **Companion blocker fixed**:
  `customerPoNumber`/`documentRecipients`/`documentRecipientMessage` (`FOLLOW_UP_FIELDS`) +
  attachments are now exempt from the PendingApproval/Final edit lock — this also un-broke
  "ส่งอีเมลแจ้งผู้รับเอกสาร" on Final records, whose save-then-send always tripped the content
  lock. Time-based auto-chasing (e.g. "remind after 3 days") still needs cron — **remains
  deferred to post-migration** per the no-Vercel-locked-services rule. See CHANGELOG.md.
- [x] ~~**⚠️ ACTION REQUIRED (2026-07-29): grant the new `scopeOfWork:chasePo` permission to existing production roles.**~~ — **done 2026-07-29**: ticked "ทวงเลข PO (ส่งแจ้งเตือนถึงพนักงานขาย)" for Administrator, Approver Level 1, and Approver Level 2 via Role Management (Sales User/Viewer deliberately not, per the default grant matrix). See CHANGELOG.md.
- [ ] **Manually verify the "ทวง PO" feature (2026-07-29) against a live deployment.** Verified
  via `tsc`/`lint`/`build` (all clean) and code review only — same standing limitation.
  Specifically unverified against real data: the chase notification actually lands in the right
  salesperson's bell and deep-links to the record; the resolution chain picks the intended person
  on records whose `quotationSalesperson` doesn't exactly match a user's `fullName`; saving the
  PO number on a real Final record works (and other fields genuinely stay locked); ส่งอีเมล on a
  Final record now works end-to-end; the list badge/filter/counter and the Dashboard "ยังไม่มีเลข
  PO" tile show correct counts.
- [x] Persist the secondary quotation document fields (contact person, phone, address, tax ID, PO reference, issue/expiry dates, payment terms) — done as part of the PDF polish work above, now real fields on `Quote`
- [x] **Dashboard KPI rework** — **done 2026-07-09**: real KPIs (Total Customers, Total Leads, Total Quotations, Total Products, Revenue, Won Deals, Lost Deals) now come from `GET /api/dashboard`; Customers/Leads correctly showed `0` until those modules shipped. **Total Customers now reflects real data as of 2026-07-14** (see the Customer Management item above) — `api/dashboard/index.ts`'s query was updated from `deletedAt: null` to `isDeleted: false` to match the redefined `Customer` schema. Total Leads still shows `0` (Lead Management remains unbuilt). See [MODULES/Dashboard.md](./MODULES/Dashboard.md).
- [x] **Dashboard date-range filter** — **done 2026-07-10**: Today/Yesterday/Last 7/14 Days/This Month/Last Month/This Quarter/This Year/Custom Range/All Time + a salesperson filter, all server-side, driving every KPI/chart/table on the page. See [MODULES/Dashboard.md](./MODULES/Dashboard.md).
- [ ] **Sidebar "Coming Soon" entry** for the remaining not-yet-built Lead module so the roadmap is visible instead of it simply not appearing — needs a design decision on whether a "Coming Soon" nav item just shows a placeholder page or is hidden until closer to ready. (Customers shipped 2026-07-14 as a real, permission-gated module — no longer in this bucket. User Management, Role Management, Notifications, and Audit Logs shipped 2026-07-08.)
- [x] Notifications module — real feed behind the header bell (`src/components/NotificationBell.tsx`), correct unread-count badge, role-based delivery for quotation events
- [x] Audit Logs module — now meaningful since real multi-user accounts exist; append-only, read-only
- [x] Decide the real behavior for Dashboard's "ส่งออกรายงาน"/"+ สร้างคำสั่งซื้อ" buttons — **decided and done 2026-07-09**: removed outright (no real Reports/Orders module exists to back them; a stub handler would just be a different kind of placeholder). Optional CSV export of real dashboard KPIs would be a reasonable low-risk future add if requested.
- [x] Wire the global header search (currently decorative on every page) — done 2026-07-14, see Global Search entries above.
- [ ] Add automated tests — **partially done 2026-07-29**: `lib/quotes.tsx` totals math is now covered (`tests/quoteAmounts.test.ts`, incl. parity with the server's `quoteAmounts.ts`); `lib/products.ts` CRUD helpers still uncovered. See High Priority above for the API-layer testing status.
- [x] ~~Add a CI pipeline (lint + typecheck + build on push)~~ — **done 2026-07-29**, see the High Priority CI item above (`.github/workflows/ci.yml`).
- [ ] **`App.tsx` never re-checks the session after initial boot, so a user whose cookie has since expired/gone invalid sees no warning until an action actually fails** (2026-07-21, from a real user report of a confusing "Not authenticated" toast on Approve — see CHANGELOG.md "Translate the raw 'Not authenticated' 401 into a real Thai/English message"). That pass only fixed the *message text* (now "เซสชันหมดอายุ กรุณาเข้าสู่ระบบใหม่" instead of the raw English server string); the UI still shows the user as logged in and lets them keep clicking things that will all fail the same way until they manually log out/in. A real fix would need either a periodic/on-focus session re-check in `App.tsx` that redirects to Sign In the moment a session goes invalid, or a global 401 handler in `apiClient.ts` that forces sign-out UI immediately on any "Not authenticated" response — neither built yet, flag to the user before doing either since both are bigger than a message fix.

## Medium Priority — Production-readiness follow-ups (2026-07-09 request)

- [x] **Translate the rest of the app's Thai UI** — **done 2026-07-09 (follow-up pass)**: sidebar/topbar, login/setup pages, Settings (all tabs), Products, Quotations (screen editing UI), User/Role Management, and Audit Log are now all wired to `useI18n()`/`t()` (~450 dictionary keys). Introduced a stable `NavKey` enum decoupled from the (now-translatable) display label, since navigation state was previously keyed off the Thai label text itself — a real bug that would have broken routing once labels became translatable. Two things remain intentionally Thai-only, not gaps: persisted data/seed content (audit log, notifications, role/department descriptions, company defaults) and `PrintDocument.tsx` (the actual printed/PDF quotation — a real business document, not app chrome). See [CLAUDE.md](./CLAUDE.md) Coding Standards.
- [ ] **Confirm `product_templates` collection semantics.** Added as schema-only scaffolding with a starting interpretation (reusable presets for fast product creation) — not confirmed with the user, don't build UI against it without checking first. See [DATABASE.md](./DATABASE.md).
- [ ] **Replace the generic department/position seed list** (ฝ่ายขาย/ฝ่ายจัดซื้อ/ฝ่ายคลังสินค้า/ฝ่ายบัญชี/ฝ่ายทรัพยากรบุคคล/ฝ่ายบริหาร/ฝ่ายไอที; พนักงาน/หัวหน้างาน/ผู้จัดการ/ผู้จัดการทั่วไป/กรรมการผู้จัดการ) with the company's real org structure once known, then build an admin UI for managing them and wire `User.department`/`User.position` (currently free text) to reference these collections instead.
- [ ] Build API routes + UI on top of the other 2026-07-09 schema-prep collections as their features come up: `quotation_comments`, `quotation_tags`, `permissions` (admin-configurable permission registry), `sessions` (session revocation / "log out other devices"), `uploads`/`attachments` (real blob storage, would also fix the base64-in-document 16MB ceiling noted below).
- [ ] Re-verify the MongoDB Atlas credential rotation item above is actually closed — it predates this pass and remains unconfirmed.

## Medium Priority — Completion-audit follow-ups (2026-07-10 request)

- [ ] **Activity Analytics** (period-grouped, multi-dimension) — a genuine gap surfaced by the 2026-07-10 completion audit (see [IMPLEMENTATION_CHECKLIST.md](./IMPLEMENTATION_CHECKLIST.md)). The Dashboard's Activity Timeline is a flat recent-N feed from `audit_log`; there's no weekly/monthly/quarterly/yearly grouping or salesperson/job-type filtering of activity counts (quotations created/edited/won/lost, status changes, approvals). Not yet scoped — reasonably buildable from existing `audit_log` + `quotes` data (audit log entries carry the actor's identity for a salesperson-style filter; job-type filtering of *activity* would need cross-referencing `details` text or isn't well-supported by the current audit log shape, worth scoping explicitly before building).
- [ ] Build a Job Type admin management UI — the `POST`/`PATCH /api/jobtypes` routes exist (`company:manage`), but there's no page; only the 13 seeded defaults are usable today.

## Medium Priority — Executive Dashboard follow-ups (2026-07-10 request)

- [x] ~~Report Export CSV for the Dashboard~~ — **done 2026-07-10** (Codex review fix pass): client-side CSV export (`src/pages/dashboard/csvExport.ts`, no new dependency) of KPIs + Sales Performance/Top Customers/Job Type Analytics tables, built from the already-filtered `DashboardStats` already on screen. "Export CSV" button in the Dashboard header.
- [ ] **Report Export — PDF** for the Dashboard still open (CSV done above; **Excel done 2026-07-24** — `src/pages/dashboard/xlsxExport.ts`, multi-sheet, reused the already-shipped `xlsx` package, no new dependency). PDF should extend the existing browser-print pattern (`PrintDocument.tsx` precedent) rather than adding a heavy new dependency.
- [ ] Real Lead/Customer entities (see the Lead & Customer Management item above) would resolve the Dashboard's free-text `client`-string-matching caveat for repeat-customer/top-customer analytics, and let the sales pipeline start at "Lead" instead of "Draft."
- [ ] A dedicated monthly quotation-*count* aggregation (as opposed to reusing the monthly *revenue* series) would make `QuotationTrendChart` show real data instead of an approximation.
- [ ] Confirm whether cross-salesperson rankings/performance data on the Dashboard should be restricted for the Sales User role (currently visible to any `dashboard:view` holder, same as every other KPI) — not built, no requirement for it yet, would need a new permission if requested.
- [ ] Build a Job Type admin management UI (the `POST`/`PATCH /api/jobtypes` routes exist, gated by `company:manage`, but there's no dedicated page — only the 13 seeded defaults are usable today, no way to add a 14th via the UI).
- [ ] **Persist a canonical pre-VAT quotation total at write time** instead of reconstructing it via `amount / 1.07` on every Dashboard request — flagged Medium by the 2026-07-14 Codex review. Correct for all current data (no intermediate rounding, `VAT_RATE` always a fixed 7%), but fragile if VAT ever becomes quote-specific or the rate changes historically. Needs a new persisted field (`subtotal`/`amountBeforeVat`) plus write-path changes in `api/handlers/quotes.ts`/`quoteValidation.ts` and a backfill decision for existing quotes — a schema migration, not a quick Dashboard fix.
- [ ] **Replace free-text salesperson/department matching with real IDs for Dashboard/activity filtering** — flagged Medium by the 2026-07-14 Codex review (same underlying gap as the pre-existing free-text-join caveats in MODULES/Dashboard.md). `Quote.salesperson`/`User.department` are free text; Sales Activity's `userName`-based actor attribution means a manager approving someone else's quote gets credited to the manager, not the salesperson. Needs `salespersonId`/`departmentId` fields referencing `User` on `Quote` and audit-log entries — a Quotation-module schema change, out of a Dashboard-only pass's scope.
- [ ] **Bound the Sales Activity Analytics audit-log query even when no date filter is selected** — flagged Medium (performance) by the 2026-07-14 Codex review. The 2026-07-14 fix bounds the query by `createdAt` whenever `from`/`to` is set, but the default (no filter) view still scans full audit-log history before bucketing/zero-filling in Node — same rolling-window convention `revenueTrend` already uses. A hard floor (e.g. cap at the widest displayed window, 5 years back) plus a MongoDB-side aggregation instead of in-Node bucketing would scale better as audit-log history grows.

## Medium Priority — Quotation Templates Codex review follow-ups (2026-07-14 request)

Zero Critical + 3 High Priority issues from this review were fixed the same day (template↔Job
Type server-side binding, 2 missing Excel source rows restored, 9 discarded real literal values
reclassified to specifications — see CHANGELOG.md). The following Medium items were explicitly
left unaddressed, deliberately out of scope for a "fix Critical/High" pass:

- [x] ~~No admin UI for Quotation Template management.~~ **Done 2026-07-15** — full `จัดการ Template ใบเสนอราคา` module (list + create/edit/duplicate/activate/archive), see CHANGELOG.md.
- [x] ~~`POST /api/quotation-templates/import` doesn't parse or hash the actual `.xlsx` file.~~ **Partially done 2026-07-15 (second fix pass)** — `api/_lib/templateWorkbookParser.ts` now really reads the workbook and hashes each sheet's content (`sourceWorkbookHash`), so a workbook edit is now genuinely detectable (real import warnings). **Still open**: the parser doesn't auto-classify parsed rows into `TemplateSection[]`/`TemplateItem[]` — `templateSeedData.ts` still needs manual re-transcription when a warning fires. Deliberately deferred (not an oversight) — direct inspection of the real workbook found rows that pack multiple different classifications into different columns of one row, too ambiguous to safely automate without risking silent content corruption. See MODULES/QuotationTemplates.md "Real Workbook Change Detection" for the concrete evidence and CODEX_REVIEW_REPORT.md "Claude Fix Status."
- [x] ~~Quote template snapshot is flattened, not structured.~~ **Done 2026-07-15 (second fix pass)** — `Quote.templateSnapshot` now holds a real, frozen-at-creation copy of the matched template's `sections`/`defaultTerms`/`internalNotes`/`sourceHash`, alongside the existing `quotationTemplateId/Name/Version` provenance strings. See MODULES/QuotationTemplates.md "Structured Template Snapshot."
- [x] ~~Template import is find-then-insert/update, not a single atomic upsert.~~ **Partially done 2026-07-15 (second fix pass)** — a concurrent-insert race no longer surfaces as an unhandled duplicate-key error (caught and treated as "skipped"). Still not a single atomic `findOneAndUpdate` — deliberately, to preserve the tested "zero writes when unchanged" guarantee an always-`$set` upsert would break. A genuinely concurrent *content-changing* race remains last-write-wins, an accepted edge case for a low-frequency admin action.
- [ ] **No real version-history/rollback for Quotation Templates.** `version` (added 2026-07-14, still true after both 2026-07-15 passes) is a plain string an admin edits by hand — there's no way to browse or restore a template's prior content. Quote-level snapshotting (now genuinely structured, see above) already guarantees existing quotations never change when a master template is edited, so this is a "nice to have" for template authoring history, not a correctness gap. Would need a `quotation_template_versions` collection (or embedded version history array) plus editor UI to browse/restore.
- [ ] **Full automated classification of parsed workbook rows into structured template content.** See the partially-done import item above — real parsing/hashing exists, but turning raw rows into `TemplateSection[]`/`TemplateItem[]` automatically (replacing hand-transcription) remains deliberately unbuilt given the demonstrated row-level ambiguity in the real workbook. Only worth building if manual re-transcription becomes a real recurring bottleneck, and should be done carefully, sheet by sheet, verified against current hand-transcribed content as ground truth.
- [ ] **Live browser/DB verification of the Quotation Templates feature (both the 2026-07-15 Template Management pass and its same-day second fix pass).** `tsc`/`lint`/`build` pass clean on both and a local Playwright check confirmed zero console errors on both, but no local MongoDB credentials means the actual list/create/edit/duplicate/archive/import/apply-to-quotation flows have never round-tripped against a real `quotation_templates`/`quotes` collection. Run the task spec's own manual test plan against a real deployment.

## Low Priority

- [ ] Dark mode — `theme.css` only defines the light palette currently; Tailwind's `dark:` variant isn't wired up.
- [ ] Revisit `DashboardPage` bundle size (~502KB gzipped ~132KB as of the second 2026-07-10 pass, up from ~490KB, mostly `recharts`) if more chart-heavy modules get added.
- [ ] Base64-in-document uploads (logo/stamp/profile picture/signature) have a practical 16MB MongoDB document ceiling — the `uploads`/`attachments` collections (schema-only, 2026-07-09) are prep for a future real blob-storage migration.
- [ ] **Verify Sales User's production role actually has `deliveryOrder:view/create/edit/print`.** Noticed in passing 2026-07-29 while granting the 4 other pending Role Management permissions (see CHANGELOG.md "Granted the 4 pending permissions") — Sales User's permission count (8) wasn't inspected in detail, and its own Delivery Order grant predates this pass by the same `defaultRoles`-only-seeds-once gap that affected the other roles, but it wasn't in the specific 4-item list actually verified this pass. A Super Admin should open Role Management → Sales User and confirm the 4 Delivery Order boxes are checked; if not, tick them.
- [ ] The per-page guided tour (`useModuleTour`, added 2026-07-29) auto-fires on a user's first visit to a page and repeatedly obscured screenshots while refreshing the user manual's images (2026-07-29) — worth a documented way to capture clean screenshots (dismiss via Escape before shooting, or seed the tour's per-user "seen" flag) if screenshotting the app becomes a recurring task.
- [ ] `CustomerAnalytics.tsx`'s stat-tile caption sits at `text-[10px]` in plain sentence case, which doesn't qualify for `DESIGN.md`'s uppercase/tracking-wide "section eyebrow" exception to the 12px chrome floor. Caught by the `/impeccable critique` detector pass (2026-07-29) on `ApprovalDashboard.tsx` and confirmed as a real (if minor) drift; the owner explicitly scoped it out of that day's fix pass. Trivial fix: `text-[10px]` → `text-xs`, matching `ExecutiveSummaryCards.tsx`'s identical caption pattern.
- [ ] `ApprovalDashboard.tsx`'s `confirmReject()` doesn't reset `busy` to `false` on its success path (only in the `catch` branch) — unlike `approve()`, which uses a `finally`. Noticed 2026-07-29 during the Approve-confirmation hardening pass; not currently observable because a successful reject triggers `onRefresh()`, which removes the row (and thus the component) from the list before the stale `busy=true` could matter. Worth a `finally` for consistency/future-proofing if this component is touched again.

## Completed

- [x] Initial Vite + React + TS + Tailwind v4 scaffold, rebranded from Figma Make source to TCS ERP
- [x] Dashboard module (KPIs, charts, leaderboard, orders, activity feed) — **superseded 2026-07-09**: leaderboard/orders/activity-feed were fake sample data with no backing module and were removed, KPIs/charts rebuilt on real MongoDB data, see entry below
- [x] Quotation module: list, create/edit, VAT/discount totals, print
- [x] Sign in / Sign up / Settings pages, wired into a real (client-only) auth gate
- [x] Product Library module: CRUD, categories, archive, duplicate, search/filter/sort/pagination
- [x] Quotation ↔ Product Library picker integration (snapshot semantics)
- [x] Quotation item notes (bullet/numbered toolbar) + unlimited reorderable sub-details
- [x] PDF/print export with proper indentation for notes/sub-details
- [x] Fixed: quotation Save/Duplicate/Send were non-functional (root-cause `Quote.lines` was never wired up)
- [x] ESLint + strict unused-code checks added and all findings fixed
- [x] Code-split by page via `React.lazy`, resolved bundle-size warning
- [x] Git repo initialized and pushed to GitHub (`Wisarutbuasumlee/tcs-erp`, private)
- [x] Full documentation system under `/docs`
- [x] Quotation PDF polish: company logo/stamp upload, real per-quote contact/document fields (no more hardcoded placeholder text), auto-hide-empty-fields in print, salesperson bound to the signed-in user, item tags, item specifications field
- [x] RBAC / User Management / Approval Workflow / Notifications / Audit Log (originally a client-side simulation, 2026-07-08; migrated to real server-side enforcement 2026-07-09 — see below): Setup Wizard, multi-user accounts, 6 roles / 17 permissions, permission-gated sidebar, User Management + Role Management admin pages, 9-status quotation approval workflow with history, signature-image integration, notification bell + role-based delivery, append-only audit log, Company gained bank/VAT/T&C fields (Super Admin only)
- [x] **Real backend migration (2026-07-09)**: Vercel Serverless Functions + MongoDB Atlas, deployed live at https://tcs-erp-nine.vercel.app — bcrypt password hashing, JWT httpOnly-cookie sessions, server-enforced RBAC on every mutating route, 8 MongoDB collections replacing `localStorage`, full REST API (`src/lib/apiClient.ts` + rewritten domain libs), superseding the never-built old "Phase 2" Next.js/Prisma/Postgres/Auth.js proposal. See [CHANGELOG.md](./CHANGELOG.md), [ARCHITECTURE.md](./ARCHITECTURE.md), [API.md](./API.md), [DATABASE.md](./DATABASE.md), [RBAC.md](./RBAC.md).
- [x] **Production-readiness pass (2026-07-09)**: official logo + shared `BrandMark` component (sidebar/login/favicon/print/loading), Dashboard fully rebuilt on real MongoDB data (zero fake stats remain), 15 new schema-prepped MongoDB collections + indexes for planned CRM/org/files/settings modules, real indexes added to all 8 previously-live collections, idempotent system-data seeding (`api/_lib/systemSeed.ts`, zero business data), 25 confirmed-dead duplicate API files + the fake `salesTeam.ts` module deleted, basic Thai/English i18n infrastructure with a Settings toggle, professional empty states on 5 pages. See [CHANGELOG.md](./CHANGELOG.md), [PROJECT_STATUS.md](./PROJECT_STATUS.md), [DATABASE.md](./DATABASE.md), [MODULES/Dashboard.md](./MODULES/Dashboard.md), [MODULES/Customer.md](./MODULES/Customer.md), [MODULES/Lead.md](./MODULES/Lead.md).
- [x] **Executive Dashboard, Sales Analytics & Job Type (2026-07-10)**: Job Type master data (13 seeded defaults, `GET/POST/PATCH /api/jobtypes`, no new permission), Potential Opportunity checkbox + Follow-up Date on `Quote`; Dashboard rebuilt from 7 KPIs/2 charts into ~20 KPIs, a sales pipeline funnel with workflow-aware conversion %, date-range + salesperson filters, sales performance/ranking tables, job type/customer analytics, a live forecast, follow-up reminders, an approval dashboard, a notification summary, an activity timeline, and 9 charts — all real MongoDB aggregation, `DashboardPage.tsx` split into 13 files. Report Export and real Lead/Customer entities explicitly deferred. `tsc`/`lint`/`build` pass clean; live-data browser verification still outstanding (see High Priority above). See [CHANGELOG.md](./CHANGELOG.md), [PROJECT_STATUS.md](./PROJECT_STATUS.md), [DATABASE.md](./DATABASE.md), [API.md](./API.md), [RBAC.md](./RBAC.md), [MODULES/Dashboard.md](./MODULES/Dashboard.md), [MODULES/Quotation.md](./MODULES/Quotation.md).
- [x] **Dashboard completion pass (2026-07-10, second pass)**: closed every remaining gap between the rebuild above and the full Executive Dashboard business spec — Pending Approvals list with inline Approve/Reject, Non-Active Jobs KPI, Won+Lost-aware Average Closing Time, Total Value alongside Won Value on Sales/Customer/Job Type tables (full 11/5/7-column, fully sortable), Revenue Trend weekly/quarterly/yearly grouping, Job Type Distribution chart, Department filter (free-text join), date-filter propagation to Follow-ups, 2 new MongoDB indexes created defensively at request time (Setup-Wizard-only `ensureIndexes()` doesn't reach production). Found via a read-only code audit against the actual spec rather than trusting the prior pass's summary. `tsc`/`lint`/`build` clean; live verification blocked by a reproducible sandbox network limitation (MongoDB Atlas SRV DNS resolution), not a code defect. See [CHANGELOG.md](./CHANGELOG.md) for the itemized list, [PROJECT_STATUS.md](./PROJECT_STATUS.md), [DATABASE.md](./DATABASE.md), [API.md](./API.md), [MODULES/Dashboard.md](./MODULES/Dashboard.md), [IMPLEMENTATION_CHECKLIST.md](./IMPLEMENTATION_CHECKLIST.md).
- [x] **Dashboard UI/UX redesign + app-wide UX enhancement pass (2026-07-10, fourth pass)**: replaced the `recharts` `FunnelChart` pipeline (overlapping Thai labels) with connected horizontal step cards (`PipelineSteps.tsx`); split the flat ~20-card KPI grid into `PrimaryKpiCards`/`SecondaryKpiSummary`; added a consolidated `QuotationStatusSummary` (Win/Lose/Active/Non-Active) and a new `SalesActivityAnalytics` section (quotation Created/Updated trend); reordered the whole page and removed 3 redundant charts (net bundle-size decrease). App-wide: shared `EmptyState`/`PageHeader`/`MetricInfoTooltip` components, sidebar regrouped into Main/Sales/Inventory/Administration, a `driver.js` guided tour (Start/Skip banner, restartable from Help), required-field indicators + inline validation on Quotation/Product/User forms, an unsaved-changes browser warning on the Quotation form. A large brief only partially covered by design (see High Priority above for what's explicitly still open: `PageHeader` rollout to other pages, tour steps for not-yet-built modules, in-app unsaved-changes interception, a full accessibility audit). `tsc`/`lint`/`build` clean; live verification blocked by the same sandbox network limitation, reproduced a third time. See [CHANGELOG.md](./CHANGELOG.md) for the itemized list, [PROJECT_STATUS.md](./PROJECT_STATUS.md), [UI_GUIDELINES.md](./UI_GUIDELINES.md), [MODULES/Dashboard.md](./MODULES/Dashboard.md).
- [x] **Audit integrity + workflow gap fix pass (2026-07-10, fifth pass)**: responds to the independent Codex re-review addendum's Critical/High findings. Quotation audit-log entries (Created/Updated/Submitted/Approved/Rejected/Status Changed) are now written server-side by `api/handlers/quotes.ts` itself (`writeQuoteAuditEntry()`, identity from the authenticated session only) instead of by the client calling the generic `POST /api/audit-log` — which any authenticated caller could previously forge, corrupting the Dashboard's Sales Activity Analytics. `POST /api/audit-log` now rejects the `"ใบเสนอราคา"` module outright. Reject/Customer-Reject/Cancel workflow actions now require a non-empty comment server-side, not just in the UI. Added the 3 missing workflow notifications (Won/Lost/Cancelled) that a creator previously never received. `tsc`/`lint`/`build` clean; a `vercel dev` browser check confirmed no new console errors (same pre-existing MongoDB DNS limitation, reproduced a fourth time) and surfaced one unrelated pre-existing gap (`App.tsx`'s stuck loading spinner on session-fetch failure, logged above, not fixed this pass). See [CODEX_REVIEW_REPORT.md](./CODEX_REVIEW_REPORT.md) "Claude Fix Status — Addendum Follow-up," [CHANGELOG.md](./CHANGELOG.md), [MODULES/Quotation.md](./MODULES/Quotation.md), [MODULES/Notifications.md](./MODULES/Notifications.md), [MODULES/AuditLog.md](./MODULES/AuditLog.md).
