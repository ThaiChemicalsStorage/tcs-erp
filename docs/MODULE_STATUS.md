# TCS ERP — Module Status

> Moved out of [`CLAUDE.md`](./CLAUDE.md) on 2026-09-21 so it stops loading into every session.
> One row per module: what is built, what was deliberately removed, and the decisions behind both.
> Each row links to its deep dive under [`MODULES/`](./MODULES/). Read it when you need the state of
> a module you have not touched recently — or before assuming a feature does or does not exist.


| Module | Status | Summary | Docs |
|---|---|---|---|
| Dashboard | ✅ Built (real data), completed against business spec + many review/fix passes | **2026-09-14: split into 7 tabs — ภาพรวม · ขาย · บริการ · จัดซื้อ · คลังสินค้า · ผลิต·โครงการ·BD (combined, owner: "แผนกไหนมีน้อยจับรวมกันเลย") · บัญชี, visible by each department's existing view permission (`src/lib/dashboardTabs.ts`), non-sales numbers from `GET /api/dashboard/departments`; same day every tab was redesigned into a chart dashboard (KPI cards with sparklines, 12-month bars, rankings, donuts) per [DASHBOARD_DESIGN.md](./DASHBOARD_DESIGN.md); which tabs a role sees is ticked per role in Role Management (`dashboard:tab*`, needs the department's document view permission too — see [RBAC.md](./RBAC.md) "Dashboard tab ticks") — see [MODULES/Dashboard.md](./MODULES/Dashboard.md) "Department Tabs". Everything below now describes the ขาย tab.** Executive BI dashboard backed by real MongoDB aggregation via `GET /api/dashboard` — compact KPI overview (**exactly 4 cards** in the top row, a documented business requirement), Quotation Status Summary, Sales Activity Analytics, Activity Timeline, plus supporting detail (Sales Performance, forecast, Scope of Work summary card). Every monetary total defaults to **pre-tax** via the shared `computeQuoteAmountBeforeVat()` helper, now user-toggleable to post-tax/incl.-VAT (`?vat=pre|post`, `computeQuoteAmountWithVat()`, a single global 7% rate either way — added 2026-08-14, see [MODULES/Dashboard.md](./MODULES/Dashboard.md) "VAT Toggle"), and every quote-count/-value aggregate collapses a rewritten quotation's revision chain to its latest revision (`dedupeQuotesByRevisionChain()`, see "Revision Chain De-duplication"). **2026-08-14**: also gained a `ServiceSummary` card (Total/Draft/Completed/Cancelled/This-Month), closing the last document module with no Dashboard presence. Date/salesperson/department filters apply server-side to most sections; the deliberate exceptions say so in the UI — see [UI_GUIDELINES.md](./UI_GUIDELINES.md) "Filter Honesty". Full pass-by-pass history (12+ passes, 2026-07-10 → 2026-07-23): [CHANGELOG.md](./CHANGELOG.md). | [MODULES/Dashboard.md](./MODULES/Dashboard.md) |
| Quotation | ✅ Built, server-validated, server-authoritative audit trail | List + create/edit/duplicate, line items with per-item notes and sub-details, print/PDF, product-library picker, **9-status approval workflow** with server-written (non-forgeable) audit entries. `amount` is always server-derived from `lines`/`discount`; numbering is atomic (a `counters` collection). Job Type classification is required + server-validated; the Create Quotation wizard applies templates (see Quotation Templates row). A Customer selector autofills from saved Customers. เงื่อนไขการชำระเงิน is free text (2026-08-31, `Combobox`) — it had been a 4-option `<select>` over a column the API always accepted free text for. A "Rewrite/แก้ไข" action creates `{root}-R{n}` revisions, with an optional one-click auto-drafted Thai Revision Note (`src/lib/revisionDiff.ts`). Required-field *policy* is deliberately minimal (only `client` required — an explicit business decision); the validation *mechanism* (server 422, real `disabled` buttons) remains. `quotations:viewAll` gates seeing others' quotes (own-quotes-only without it) — **required a manual Role Management step on production**, see [RBAC.md](./RBAC.md). History: [CHANGELOG.md](./CHANGELOG.md). | [MODULES/Quotation.md](./MODULES/Quotation.md) |
| Product Library | ✅ Built | Product + category CRUD, archive (soft-delete), search/filter/sort/pagination, duplicate, feeds the Quotation line-item picker as independent snapshots. **2026-08-18**: gained a lightweight Stock module — `Product.stockQty` + an append-only `stock_movements` ledger, a standalone "สต๊อกสินค้า" page, and a dual-pane stock-cutting view on Accounting's Tax Invoice (IV) list — not a full standalone Inventory module (no POs, no warehouse/location tracking). **2026-09-09**: the product picker **stopped hiding products** — every catalog surface filtered on a hardcoded list of four Thai category *names*, so an item the store coded into any other category vanished from ใบเบิก/ใบขอซื้อ for good (and the quotation picker, filtering the same list inverted, lost anything coded into a store category). The filter is now a visible category dropdown on the one shared `ProductPickerModal`, store categories merely sort first; the picker also shows on-hand quantity and can raise a product-code request for what it cannot find. A code assigned by the store is now **written back onto the source ใบขอซื้อ line automatically**, so nobody has to delete the line and pick it again. Also **`Product.lastCost`** — goods coming back into stock book at the latest purchase price instead of the moving average, shown on both the requisition and the stock page. **2026-09-04**: gained **Excel import** (`POST /api/products/import`) so a catalog can be moved over from another system in one drop — headers matched by name in Thai or English, a preview before anything is written, existing codes skipped rather than overwritten (case-insensitively), categories created by name, and `stockQty` forced to 0 because stock may only move through a `StockMovement`. | [MODULES/Product.md](./MODULES/Product.md) |
| Auth (Setup Wizard + Sign in) | ✅ Built (server-verified) | First-run Setup Wizard creates the Super Admin; bcrypt+JWT login verified server-side; no public self-signup. **2026-07-21**: `apiClient.ts` now translates the raw `"Not authenticated"` 401 server string (thrown whenever a session cookie is missing/expired/invalid — 7-day **rolling** JWT, or the user deactivated/edited since login) into a real message ("เซสชันหมดอายุ กรุณาเข้าสู่ระบบใหม่"/"Your session has expired — please sign in again") — previously leaked untranslated to the UI, reported by a user as a confusing "not authentication" toast on Approve. **2026-07-31**: session expiry became rolling/sliding — every API request re-issues the cookie with a fresh 7-day window (`refreshSessionCookie()` in `api/_lib/auth.ts`), so an actively-used session no longer force-expires; only 7 full days with zero requests logs the user out. `App.tsx` still doesn't proactively re-check the session mid-page-view (tracked in `docs/TODO.md`).  **2026-08-31**: sessions became **database-backed** — the JWT carries a `sid` claim naming a row in the `sessions` collection (declared since the MongoDB migration, written to for the first time), checked on every request beside the existing `status === "active"` lookup. Logging in revokes the user’s other sessions, which is the owner’s "1 user จำกัดเข้าได้แค่ 1 คน"; the evicted device is told it was **superseded** rather than merely expired, and an idle tab learns within 45s off the existing notification poll. Two latent bugs closed on the way: `expiresAt` was a `string`, so the TTL index had never purged anything, and `refreshSessionCookie()` re-signs on every request, which would have dropped any claim it forgot to copy. Side effect: closes the "no true session revocation" gap [RBAC.md](./RBAC.md) had carried since 2026-07-09. ⚠️ pre-2026-08-31 cookies have no `sid` and are refused — everyone signs in once more after deploy. | [MODULES/Auth.md](./MODULES/Auth.md) |
| Settings | ✅ Built | Self-service profile (incl. picture upload + **2026-08-13: draw-or-upload personal signature** via the shared `SignaturePad.tsx` — the only caller with the Upload mode enabled; Service's customer sign-off reuses the same component but stays draw-only, `allowUpload={false}`), Super-Admin-only company info (incl. bank/VAT/T&C), security (real password change — the short-lived 2026-08-07 Gmail App Password card was removed the same day with the email feature), notification toggles | [MODULES/Settings.md](./MODULES/Settings.md) |
| User Management | ✅ Built | Create/edit users, reset password, activate/deactivate, assign role/department/position. **2026-07-23**: `department` changed from a free-text input (datalist autocomplete hints only) to a real `<select>` constrained to 6 values (Purchase/Project/Factory/Technic/Service/Accounting, `DOCUMENT_RECIPIENT_DEPARTMENTS` in `src/lib/documentRequirements.ts`) — links a user's department to Scope of Work's "เอกสารส่งถึง" document-routing checklist, see [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) "Document Recipients". A pre-existing free-text value is kept as a selectable "legacy value" fallback option rather than silently discarded. | [MODULES/UserManagement.md](./MODULES/UserManagement.md) |
| Role Management | ✅ Built | Create/delete custom roles, edit permission matrix — Super Admin only | [MODULES/RoleManagement.md](./MODULES/RoleManagement.md) |
| Notifications | ✅ Built | Header bell with unread badge + panel, role-based delivery for quotation events, click deep-links to the specific quotation (2026-07-10). **2026-07-10, fifth pass**: added the 3 missing terminal-workflow notification types (Won/Lost/Cancelled). **2026-07-23**: added `scope_of_work_document_sent`, the first notification type not tied to the quotation approval workflow — fired to explicitly-picked people (not a role) when Scope of Work's "ส่งอีเมลแจ้งผู้รับเอกสาร" runs, deep-links to that record on the standalone Scope of Work page via a new `relatedScopeId` field. See [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) "Document Recipients". **2026-07-24**: the client now polls `GET /api/notifications` every 45 s (plus refetch on tab focus) so new notifications appear without a manual page reload — polling deliberately, not SSE (chosen while the app still ran serverless; SSE is a recorded possible upgrade now that it runs on its own server, see [SERVER_MIGRATION_PLAN.md](./SERVER_MIGRATION_PLAN.md)).  **2026-08-31**: gained the six `*_submitted` types the shared approval engine now fires when a document is sent for approval — recipients resolved by **approve permission**, not department (the owner’s reason for asking was that one head approves across several), plus the four deep-link fields those documents lacked (`relatedJobOrderId`/`relatedProductionOrderId`/`relatedPurchaseOrderId`/`relatedCostControlId`), without which the bell would show a notification that goes nowhere. | [MODULES/Notifications.md](./MODULES/Notifications.md) |
| เอกสารรออนุมัติ (Pending Approvals) | ✅ Built (2026-08-31) | หน้ารวมทุกใบที่รอ**คุณ**อนุมัติ จากทุกแผนก — asked for because *"เฮดคนนึงต้องอนุมัติหลายแผนก"*. One `GET /api/pending-approvals` fans out over **10 document types** and reconciles the **three different spellings of "รออนุมัติ"** the database actually holds (`PendingApproval` ×8, `รออนุมัติ` for quotations, `Pending` for product requests) — a full-database migration to unify them was rejected in favour of translating at the API. Gated by each document’s **approve** permission, not its view permission, and deliberately **not** ownership-filtered (an approver filtered by `createdBy` would see only what they wrote themselves). **No permission and no RBAC migration of its own** — the menu opens on `anyPermission`, a new `NavCandidate` field, because a `pendingApprovals:view` would be one more box an admin must tick to unlock a page granting no new authority, which is precisely how จัดซื้อ/BD stayed invisible for three days. Rows deep-link to the document; there is deliberately no combined approve button. ⚠️ the "รอมานาน" column is `updatedAt` for 9 of 10 types — no `submittedAt` exists in this system (quotations excepted, via `approvalHistory`). | [MODULES/PendingApprovals.md](./MODULES/PendingApprovals.md) |
| Audit Log | ✅ Built, quotation events non-forgeable | Append-only, read-only log of every sensitive action. **2026-07-10, fifth pass**: `POST /api/audit-log` now rejects the quotation module — those events can only be written by the quote handlers themselves. | [MODULES/AuditLog.md](./MODULES/AuditLog.md) |
| Lead Management | ⚠️ Schema only | MongoDB collections (`leads`, `lead_activities`) + indexes exist as of 2026-07-09 prod-readiness pass; no API routes or UI yet | [MODULES/Lead.md](./MODULES/Lead.md) |
| Customer Management | ✅ Built (2026-07-14) | Customer master data (company/contact/phone/email/address/tax ID/delivery/project fields), admin CRUD page (`src/pages/customers/CustomersPage.tsx`), 4 new permissions, and a selector on the Quotation form that autofills Customer Information from a saved customer. Redefines the earlier (2026-07-09) schema-only `customers` shape — no live data existed under it, so no migration was needed. `customer_contacts` remains schema-only. | [MODULES/Customer.md](./MODULES/Customer.md) |
| RBAC / Admin (server-enforced) | ✅ Built | 2026-07-09: migrated from client-side simulation to real server-side enforcement (Node.js API + MongoDB, bcrypt + JWT auth, `requirePermission()` on every mutating route) — see rows above for the UI/UX, unchanged by the migration | [RBAC.md](./RBAC.md) |
| Departments & Teams | ✅ Built (2026-08-14) | Direct business request: Sales has 2 teams, each with its own team lead who should see only their own team's records. Wires up the previously-schema-only `departments` collection (real admin CRUD page, `departments:manage`) and adds a new `teams` collection (sub-grouping within a department, `teams:manage`) — see `src/pages/admin/DepartmentManagementPage.tsx`. Adds a 4-tier visibility cascade (own → team → department → all) to Quotations/Scope of Work/Delivery Order via 6 new `{module}:viewTeam`/`{module}:viewDepartment` permissions and a shared `buildOwnershipClause()` helper (`api/_lib/visibility.ts`), also wired into the Dashboard's own-data scoping. User Management's department dropdown now sources from this real collection instead of a hardcoded constant; a new team dropdown sets `User.teamId`. No hardcoded team-lead role — created via existing Role Management once deployed. | [MODULES/Department.md](./MODULES/Department.md), [RBAC.md](./RBAC.md) |
| Global Search (topbar) | ✅ Built (2026-07-14) · **every document + centred panel (2026-08-28)** | **2026-08-28, direct owner request ("ค้นหาได้ทุกเอกสาร กดไปละไปดูในเอกสารได้เลย"): search went from 7 categories to 16** (19 once the Purchasing module landed later the same day, then 17 after ใบตรวจรับ/ใบรับวางบิล were withdrawn), adding every business document it had been blind to — ใบส่งมอบสินค้า, รายงานบริการ, โครงการ, ใบเบิกและใบคืนวัสดุ, ใบสั่งงาน, ใบขอซื้อ, ใบสั่งผลิต, คำขอเพิ่มสินค้า, and all 4 accounting document types. Each new category is permission-gated and ownership-scoped to match that module's own list route exactly (`api/_lib/searchDocuments.ts`), and all 12 document categories share one `SearchDocumentResult` shape so the UI renders one row component rather than twelve. A **document-number fast path** (`Q#` / `MR-` / `JO-` / `PR-` / `PO-` / `SC-` / `SR-` / `AR`,`BI`,`RE`,`IV`) runs a single anchored prefix query — the only index-usable shape in a system with no text index — and pins the hit pre-selected so Enter opens it. `?types=` plus filter chips narrow to one kind (3-row teaser → 20). The UI is now a **centred modal panel** (full-screen below `sm`), replacing both the `w-[26rem]` dropdown and the separately-rendered mobile takeover; every result opens that document on its own module page, which required adding the missing `navigateToProductionOrder()` (dead wiring since the module was built) and deep-linking for accounting documents (none existed at all). The same pass **aligned search's visibility with the list pages** — it had used a binary own/viewAll rule while the list routes moved to the 4-tier cascade on 2026-08-14, so a team lead saw a teammate's quotation on the list page but couldn't find it in search. First test coverage: `tests/api/search.test.ts` (15 tests). **2026-07-14 original**: replaced a never-functional dead search input; `GET /api/search?q=` searched Quotations/Customers/Products/pages/Users, every category independently RBAC-filtered server-side; debounced dropdown with keyboard nav and a Ctrl/Cmd+K shortcut. Same-day Codex-review fixes: server-side max query length, a real mobile entry point, view-only customer results, combobox/listbox ARIA. Not a standalone admin module — no dedicated page or permissions of its own, just a cross-cutting search over the modules above. | [API.md](./API.md), [UI_GUIDELINES.md](./UI_GUIDELINES.md) |
| What's New (topbar) | ✅ Built (2026-07-23) | Direct user request for an in-app "update log" so users can see recent feature updates without asking anyone — a "มีอะไรใหม่" sparkle icon in the topbar (`src/components/WhatsNewPanel.tsx`, next to the notification bell) opens a dropdown of recent updates, newest first, each with a date and a short Thai bullet-list description. Content is a plain hand-maintained array (`WHATS_NEW_ENTRIES` in `src/lib/whatsNew.ts`) — no new MongoDB collection or API route; these are short end-user announcements, not the technical changelog (`docs/CHANGELOG.md`), so they're authored directly rather than auto-generated from it. A gold dot badge shows when there's an entry newer than the ones this specific user has already opened the panel to see — tracked per-user in `localStorage` (`hasUnseenWhatsNew()`/`markWhatsNewSeen()`), the same client-side-preference convention `tour.ts` already uses for guided-tour completion, not real business data so no schema/API change was warranted. Not a standalone admin module — no dedicated page/permissions, just a cross-cutting announcement panel like Global Search above. To add a new announcement: prepend an entry to `WHATS_NEW_ENTRIES`; only genuinely user-relevant features belong here, not every internal fix. | [UI_GUIDELINES.md](./UI_GUIDELINES.md) |
| Company Profiles | ❌ Removed (2026-07-14) | Built 2026-07-13 as business-identity master data (name/logo/address/tax ID/branch/bank accounts), briefly (2026-07-13–14) mis-wired into the Quotation form as an "issuer company" selector, then the same day reverted and replaced with the correct feature (a **Customer** selector — see [Customer Management](./MODULES/Customer.md) row above). Later on 2026-07-14 the standalone admin module itself was also removed on explicit instruction — this ERP only ever needs one issuer company, so a page for managing several was unused scope. UI/routes/permissions/i18n keys all removed; `company_profiles` MongoDB collection and any historical documents/audit entries left untouched (no destructive DB cleanup). See [MODULES/CompanyProfiles.md](./MODULES/CompanyProfiles.md) for the full removal writeup and CHANGELOG.md for the itemized diff. | [MODULES/CompanyProfiles.md](./MODULES/CompanyProfiles.md) |
| Service | ✅ Built (Phase 1, 2026-08-06) | Field-service checklist + report module ("บริการ") — created directly against a Customer, not derived from a Quotation (unlike every module above). Two seed checklist templates transcribed from real reference files (moved 2026-08-17 to `reference/company/รายการตรวจเช็ค.pdf`/`reference/company/Service.xlsx` — see the "Move real customer/company documents out of public/" 2026-08-17 CHANGELOG entry; content already transcribed into code, these are historical provenance only), atomic `SR-{year}-{seq}` numbering, a frozen `templateSnapshot` per report (editing the master template never retroactively changes an already-created report), a required-abnormalDetail-and-photo rule when a checklist item is marked Abnormal, and a printable A4-portrait report (browser-native CSS, no PDF library in this app) — the photo attachments and print view shipped the same day as Phase 1, pulled forward from the original Phase 2/3 roadmap per direct user request. Own standalone "บริการ" permission group (11 permissions). **2026-08-07**: a 7th default role, **Service Engineer**, now ships in `defaultRoles` (own reports only: `service:view/create/edit/complete/print` + `serviceTemplates:view` + `customers:view` + `dashboard:view`), and the long-standing "grant the new permissions manually in Role Management after deploy" step that every module since Scope of Work has needed is **gone** — `bootstrapRbac()` (`api/_lib/rbacSeed.ts`) inserts later-added default roles and applies a **once-only, recorded** permission backfill (`rbac_migrations` collection) so a permission an admin revokes stays revoked. Signature capture, mobile/iPad UX, and customer acceptance (on-site + a time-boxed remote link) remain later, not-yet-built phases. | [MODULES/Service.md](./MODULES/Service.md) |
| Quotation Templates | ✅ Built, incl. the full Template Management admin module | Create Quotation wizard (Job Type → Template → Preview → pre-filled form) + a full `จัดการ Template ใบเสนอราคา` admin module (sections/items CRUD, duplicate, activate/archive, 7 granular permissions). Templates live in `quotation_templates`, imported from the real source workbook via an idempotent content-hash-gated `POST /api/quotation-templates/import` (`api/_lib/templateWorkbookParser.ts` does real `.xlsx` parsing). Copy-on-apply is **"snapshot, never live reference"** — internal-staff notes never reach a customer quote, and `Quote.templateSnapshot` freezes the applied template's structure at creation time. Template product links are server-resolved/snapshotted, never trusted from the client. The wizard's Job Type grid uses a documented 4-way ordering partition (has-Template → no-Template → OTHER BF/SC/TA → generic OTHER). History incl. two same-day Codex-review fix passes: [CHANGELOG.md](./CHANGELOG.md). | [MODULES/QuotationTemplates.md](./MODULES/QuotationTemplates.md) |
| Scope of Work | ✅ Built | Document type generated from a quotation, reproducing the reference PDF in `public/` — an **independent snapshot** (editing never touches the quotation; explicit "อัปเดตข้อมูลจากใบเสนอราคา" re-pull), **no pricing anywhere**, **manually-typed document number** (2026-07-29: free-form, required, server-checked unique, Draft-only editable — auto-generation removed, see [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) "Scope Number / Job Code"), 11 reusable checklist groups, strict required-field validation before Print/Finalize, Draft/Final lifecycle, Duplicate + Rewrite (`-R{n}` on `scopeNumber`) with auto-draftable Revision Note, and its own standalone sidebar page. 7 `scopeOfWork:*` permissions incl. `viewAll` (own-records-only without it) — **required a manual Role Management step on production**, see [RBAC.md](./RBAC.md). **Document Recipients**: the "เอกสารส่งถึง" checklist maps to real people via department-linked pickers, plus a checklist-independent "ผู้รับเพิ่มเติม" any-user picker (2026-08-07); "ส่งแจ้งเตือนผู้รับเอกสาร" is **in-app-notification-only since 2026-08-07** (email sending removed entirely the same day it briefly became person-to-person Gmail — no setup/env var needed anymore) — it writes the bell notification and makes the record visible to recipients. **Attachments** (≤2 MB/file, ≤5/record) store bytes in the `scope_attachment_files` MongoDB collection with unauthenticated capability-URL downloads — deliberately **not** an external blob store, see [SERVER_MIGRATION_PLAN.md](./SERVER_MIGRATION_PLAN.md); not carried over by Duplicate/Rewrite. **Multiple quotation/PO numbers (2026-08-31)**: one Scope can carry several of each (`additionalQuotationNumbers`/`additionalPoNumbers` + "+ เพิ่ม" buttons), stored as "primary + extras" rather than one array so the dashboard "ยังไม่มี PO" count and the refresh-from-quotation button keep working unchanged — see [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md). **PO chasing (2026-07-29)**: "ยังไม่มี PO" badge/filter/counter (list + Dashboard) + a repeatable audit-logged "ทวงเลข PO" in-app notification to the record's salesperson; PO number/recipients/message/attachments are **exempt from the Final lock** (follow-up data — see [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) "PO Chasing"). History (10+ passes, 2026-07-15 → 2026-07-24): [CHANGELOG.md](./CHANGELOG.md). | [MODULES/ScopeOfWork.md](./MODULES/ScopeOfWork.md) |
| Accounting (AR / Milestone Billing) | 🟢 Phase 1 (2026-08-17) + Phase 1.5 (2026-08-18) built | Accounts Receivable billing hung off Scope of Work payment installments. Phase 1: `ar_milestones`/`ar_documents` collections, atomic Buddhist-year `{PREFIX}{YY}{MM}{SEQ}` numbering matching the company's real "Express" software, checklist/attachments per milestone, job-centric issuing (AR-or-IV + companion BI in one action), basic cancel, 4 `ar:*` permissions + seeded `accounting_user` role. Phase 1.5 (owner's follow-up, "1 ใบคือ 1 หน้า"): **each document type is its own sidebar page** — ใบรับเงินมัดจำ/ใบกำกับภาษี (AR), ใบแจ้งหนี้/ใบวางบิล (BI), ใบเสร็จรับเงิน (RE, new document type issued once per paid tax invoice, closes its milestone), ใบกำกับภาษี/ใบส่งสินค้า (IV) — plus a deposit-billed alert on the job list/detail and a monthly "สรุปเอกสารประจำเดือน" page for VAT-filing checks. A dedicated "แดชบอร์ดบัญชี" page (KPIs, 12-month trend, doc-type breakdown, AR aging, billing-status funnel, top customers) shipped the same day. Also same day: IV-only stock-cutting (dual-pane `ArStockPanel`, tied to a new shared Product Stock module — see [MODULES/Product.md](./MODULES/Product.md) "Stock") and Manual Tax Invoice creation for AR/IV (`POST /api/ar-documents/manual`, a "+ create" button matching Quotation's style, no Scope of Work required — BI/RE deliberately excluded, they always reference a principal invoice). NCR/dot-matrix print calibration and WHT/retention reconciliation remain later phases. | [MODULES/Accounting.md](./MODULES/Accounting.md) |
| Production (ผลิต) | ✅ Built (2026-08-20) | ใบสั่งผลิต (Production Order, FM-PD-02 — the one new document type, generated directly from an approved Scope of Work and numbered SC-YYYY-MM-NNN on the Gregorian year to match the real form) plus the Production departments own ใบเบิก-คืนวัสดุ / ใบขอซื้อ, separated from the Project departments records by ownerDepartment. ใบส่งมอบงาน deliberately reuses the existing Delivery Order module. Also introduced the shared ร่าง→รออนุมัติ→อนุมัติ approval workflow now used by four documents. 7 new productionOrder:* permissions. | [MODULES/Production.md](./MODULES/Production.md) |
| Store (แผนกสโตร์) | ✅ Built (2026-09-03) · **หน้าตัดของของสโตร์ (2026-09-10)** · **ใบเบิก/ใบรับคืนแยกรหัส + ใบรับสินค้าใบเปล่า RR/RX/RI + ประวัติสต๊อก + นำเข้า Excel (2026-09-23)** | The Store department's own feature set, from a single 8-point list ending *"ช่วยทำในส่วนตัวนี้ขึ้นมาให้หน่อยเป็นของแผนกสโตร์"*. **ใบรับสินค้า (RR)** — one per approved PO (unique partial index, not just a code check), several receipt rounds inside the one document, where each round writes stock **with cost**, posts one `ap_entries` payable, and closes the document when nothing is outstanding; a hand-typed line posts a payable but no stock; the newest unpaid round can be reversed, rolling back both sides. Two new accounting pages read that ledger: **ทะเบียนภาษีซื้อ** and **ทะเบียนเจ้าหนี้** (month filtered on the *invoice* date). The requisition **stopped cutting stock at approval and started cutting when Store actually issues** (partial issues allowed, outstanding shown per line, charged to a department/team/work-type that is stamped onto every movement). Stock gained a moving-average `avgCost`, a `"return"` movement kind, stock value, and a printable **stock card**. **เครื่องมือประจำทีม** aggregates the same ledger with no collection of its own, and (2026-09-03b) issues/takes back tools straight to a team with its own `TL-YYYYMM-NNNN` slip — still the same ledger, only a different `sourceType`. All internal document numbers became **`{PREFIX}-{YYYYMM}-{NNNN}`**, restarting monthly in Bangkok time — the letter prefix is load-bearing for Global Search's fast path. 9 new permissions with an RBAC migration; 47 new tests. **✅ Clicked through in a browser 2026-09-03c** (separate port + throwaway database), which found two client-side staleness bugs no API test could see; **✅ Print layouts checked as real PDFs and the English UI swept 2026-09-04**, which found two more DOM-invisible bugs (the purchase tax register printed without its three money columns; the letterhead ribbon overflowed the page box and clipped every serif-font printout on the right). The RR print layout is still a placeholder pending the owner's real form. **2026-09-10**: Store got **a queue page of its own, "ตัดของตามใบเบิก"** — the requisition list is mounted once per department and shows only that department's documents, so there was no single place answering *"which requisitions are waiting for me to pick?"*. The new page holds every approved requisition with something still to issue, **both departments together**, oldest first, and a row expands in place into the same issue form so goods go out without opening the document at all; a fully-issued requisition leaves the queue on its own. It posts the same `POST /:id/issues` route under the same `stock:adjust` gate — not a second way to move stock — and is the second inbox (after Pending Approvals) that deliberately **does not filter by `createdBy`**, since ownership filtering would show Store nothing. No new permission and no RBAC migration: the menu needs `stock:adjust` + `materialRequisition:view`, which any role that can already issue holds. | [MODULES/Store.md](./MODULES/Store.md) |
| Purchasing (จัดซื้อ) | ✅ Built (2026-08-28) · **ใบขอซื้อผ่านสโตร์ก่อนถึงจัดซื้อ (2026-09-09)** | The Purchasing department's first module of its own — before this it existed only as a signature box on ใบขอซื้อ and one notification recipient. Built from the owner's "กระบวนการจัดซื้อ" flow chart: **ใบสั่งซื้อ (PO)** — created from an approved ใบขอซื้อ (line snapshot, never a live reference) or blank, on the shared ร่าง→รออนุมัติ→อนุมัติ engine with `-R1` rewrite. Also **opened ใบขอซื้อ to every department** via a third `ownerDepartment: "general"` and a create path with no source document, so สโตร์/เซอร์วิส/บัญชี/บุคคล can raise one at all — plus a view-only `?ownerDepartment=all` scope behind a new "ใบขอซื้อ (ทุกฝ่าย)" inbox listing every department's requests with a แผนก column (department wall lifted, ownership wall untouched). A **ninth nav group** (a documented departure from DESIGN.md, at the owner's choice), 7 permissions, a `PO-` search category, and `tests/api/purchasing.test.ts` (9 tests). Mounts on `api/handlers/quotes.ts`. **⚠️ ใบตรวจรับสินค้า (GR) and ใบรับวางบิล (BR) were built the same day and removed hours later** on the owner's instruction ("ไม่ได้ใช้") — see [MODULES/Purchasing.md](./MODULES/Purchasing.md) "Removed 2026-08-28"; the code is gone, the two empty MongoDB collections were left untouched. **2026-08-31**: ใบขอซื้อ's print layout was matched against the real filled FM-PU-05 (`ED6908038.pdf`), and the department gained a **ทะเบียนผู้ขาย** (vendor register, `vendor:*` ×4) whose codes are unique-when-set — plus a shared `src/components/Combobox.tsx` (type freely, suggestions appear) first used for the vendor field on a PO, and a **ทะเบียนรหัส** (code register, `codeRegister:*` ×4) holding two unrelated code sets in one collection keyed by `kind` — department codes (`G143`) and the 479-row chart of accounts (`5230-15`, importable from the owner’s Excel), wired into the `departmentCode`/`costCode` boxes on both ใบขอซื้อ and ใบสั่งซื้อ (`costCode` had been a stored-but-uninputtable dead field since 2026-08-27). The same pass shipped the RBAC migration that restored `purchaseOrder:*`/`costControl:*`, which had never reached any existing role, leaving both nav groups invisible to everyone but Super Admin. **ใบสั่งซื้อ's print layout is still a placeholder** pending the owner's real form. **2026-09-09**: ใบขอซื้อ gained the middle step the owner's flow always had — **อนุมัติ → สโตร์เช็คของ → จัดซื้อ**. Store marks each line in-stock or to-be-bought, issues the in-stock ones straight off the purchase request (real stock movements, `sourceType: "purchase_request"`, append-only rounds, cancel-latest), and only what is genuinely missing reaches Purchasing — a PO cannot be raised while a request is still with Store, and store-issued lines are never copied onto it. Modelled as **fields on the purchase request (`storeStage`), not a fourth `ApprovableStatus`**, because that union is shared by six documents; requests created before that date carry no field and keep going straight to Purchasing. Same pass: **จัดซื้อ can edit an approved ใบขอซื้อ** (new `purchaseRequest:editApproved`, every field as if it were a draft, each save appended to `purchasingEdits[]` + audit + a notification to the author; `PendingApproval` stays locked for everyone and auto-save is refused with 409). ⚠️ the RBAC migration can only reach `administrator` — the Store and Purchasing roles are customer-created, so two permissions must be ticked by hand once (see [TODO.md](./TODO.md)). | [MODULES/Purchasing.md](./MODULES/Purchasing.md) |
| Cost Control (BD) | ✅ Built (2026-08-28) | รวบรวมต้นทุนทุกบรรทัดของงานไว้ในเอกสารเดียว · **เดิมคิดกำไรให้ด้วย (ต้นทุน + ค่าดำเนินการ/Bubble/Entertainment เทียบราคาขาย) — เจ้าของสั่งถอดออกทั้งหมดเมื่อ 2026-08-31 แล้วสั่งเพิ่มให้เอายอดต้นทุนรวมออกด้วย** ตอนนี้**ไม่มีการรวมยอดที่ไหนเลย** ทั้งใบพิมพ์ หน้าเอกสาร และหน้ารายการ เหลือแต่ต้นทุนรายบรรทัด (ใบพิมพ์จึงต่างจากฟอร์ม FM-SL-06 ตรงจุดนี้ โดยตั้งใจ) · ข้อมูลเก่าที่เคยกรอกไว้ยังอยู่ใน MongoDB ไม่ได้ถูกลบ แค่ไม่มีใครอ่าน **ปิดช่องว่างที่ค้างมาตั้งแต่ 2026-08-20** ซึ่งถูกจดไว้ 5 ที่ว่า Cost Control สร้างไม่ได้เพราะยังไม่มีใครนิยามว่ามันคือข้อมูลอะไร — คำตอบคือไฟล์ Excel ที่บริษัทกรอกมืออยู่แล้ว โมดูลนี้จึง**ถอดแบบจากไฟล์จริง**ทั้งโครงข้อมูลและใบพิมพ์ สร้างได้ 2 ทาง: **โยนไฟล์ Excel ของงานเข้ามา** (เบราว์เซอร์แกะเอง ส่งขึ้นเซิร์ฟเวอร์เป็น JSON ที่คนตรวจแล้ว — ไม่มีการอัปโหลดไฟล์ดิบ) หรือ **เปิดใบเปล่ากรอกเอง** · การแปลงจากใบประเมินราคา (ชีต `SC`) **ไม่ใช่ 1:1** — คนที่ทำใบจริงทั้งแตกและยุบบรรทัดตามดุลพินิจ ตัวแกะจึงให้แค่จุดตั้งต้นและ**บังคับผ่านหน้า preview ที่แก้ได้เสมอ** · ชนิดแถว (หัวกลุ่ม/รายการ/รายละเอียดย่อย) อ่านจาก**สีพื้นหลังของเซลล์** เพราะในไฟล์จริงตัวหนังสือของหัวกลุ่มกับบรรทัดบรรยายเหมือนกันเป๊ะ · **ไม่เก็บยอดรวมลงฐานข้อมูลสักตัว** ทุกยอดคำนวณจาก lines ตอนอ่าน · ใบพิมพ์เป็น**ฟอร์มจริง FM-SL-06 Rev.02** ไม่ใช่ placeholder (ต่างจากใบสั่งซื้อ) · กลุ่มเมนูที่ 10 "BD" · 7 สิทธิ์ `costControl:*` · หมวดค้นหา `CC-` · เทสต์ 59 ข้อ (`tests/costControlImport.test.ts` 49 + `tests/api/costControl.test.ts` 10) · **รู้จักชีตจากเนื้อในไม่ใช่ชื่อชีต และเลือกได้หลายชีตพร้อมกัน** (2026-08-28g) · **2026-08-31: ไม่นำราคาจากไฟล์เข้ามาอีกต่อไป** — `stripImportedPrices()` ล้างต้นทุนทุกบรรทัดและบล็อกสรุปทิ้ง เหลือแต่ตัวรายการให้คนกรอกราคาเอง (เจ้าของสั่ง: ราคาในไฟล์ประเมินมักยังไม่ใช่ต้นทุนจริง) · ตัดที่ชั้น dialog ไม่ใช่ที่ตัวแกะไฟล์ เพื่อรักษาคำเตือน "อาจมีบรรทัดที่อ่านไม่เจอ" ซึ่งคำนวณจากราคาที่อ่านมา · **2026-08-31i: ผูกกับ Scope of Work ได้แล้ว** (`scopeOfWorkId` เป็น FK จริง อยู่ข้าง `jobOrder` ที่ยังเป็นข้อความ) เจ้าของสั่งว่า *"Scope of work เวลาที่จะส่งไปให้คนอื่นอะมันจะมาพร้อมกับ Cost control ด้วย"* — งานหลักจึงเป็น**ทางมองเห็นทางที่สาม**ของโมดูล: ผู้รับเอกสารของ Scope เห็นใบต้นทุนที่ผูกอยู่ทั้งในรายการและ Global Search **ดูและพิมพ์ได้ แก้ไม่ได้** (`assertNotScopeRecipientOnly()` กันแม้บทบาทจะถือ `:finalize`) · ⚠️ ไม่แจกสิทธิ์ให้ใคร ผู้รับต้องมี `costControl:view` อยู่แล้ว | [MODULES/CostControl.md](./MODULES/CostControl.md) |
| Delivery Order | ✅ Built (2026-07-23) | "ใบส่งมอบสินค้าและบริการ" generated from a Scope of Work (independent snapshot; explicit re-pull reconciles installments by id), with its own standalone sidebar page. Printing is strictly **per payment milestone** — each installment card has its own print button producing an independent Delivery Note (own เลขที่/วันที่, ticked-items checklist, Remark); there is deliberately no whole-document print. Deposit installments are excluded via the exact-whole-label `isDepositLabel()` set ("Down Payment"/"Deposit"/"เงินมัดจำ"/"ชำระเงินล่วงหน้า") — **never substring/percentage-based**. The print layout is a visually-verified replica of the FM-SL-05 reference PDF, and the browser's print-dialog URL footer is suppressed via a component-scoped `@page { margin: 0 }` override (the one documented exception to the "browser headers/footers aren't app-controllable" rule — see [UI_GUIDELINES.md](./UI_GUIDELINES.md) "Print / PDF"). 7 `deliveryOrder:*` permissions mirroring Scope of Work — **required a manual Role Management step on production**. No Duplicate/Rewrite and no required-field gate (both deliberate). History: [CHANGELOG.md](./CHANGELOG.md). | [MODULES/DeliveryOrder.md](./MODULES/DeliveryOrder.md) |
| Project | ✅ Built + live-verified (all 4 document types, 2026-08-18) | Generated from a Scope of Work (own "สร้าง/เปิดโครงการ" toolbar button on `ScopeOfWorkDocument.tsx`, same existence-check pattern as Delivery Order's own button) — distributes every Scope of Work item across 3 sourcing branches (in stock via **Material Requisition**/FM-ST-04, fabricated in-house via **Job Order**/FM-PJ-01, purchased externally via **Purchase Request**/FMPU05), tracking status per item. Creating a sub-document atomically links it back onto its `ProjectItem` (`sourcingMethod`/`itemStatus`/id — server-derived, covered by `tests/api/projectAtomicity.test.ts`). Material Requisition lines reference the existing `Product` catalog (82 real store items seeded from the real FM-ST-04 reference PDF into 4 new categories, `tests/api/materialCatalogSeed.test.ts`); Purchase Request lines optionally do the same. Job Order/Purchase Request built Stage 5, each with its own standalone sidebar page and full Draft/Final workflow; Material Requisition's "คืนของ" (return) column stays editable even once Final. All 4 document types **fully i18n-wired** — the 4 print documents and the catalog/checklist content stay fixed-Thai by deliberate precedent, see [MODULES/Project.md](./MODULES/Project.md) "i18n". **A 5th document, Work Handover Note (ใบส่งมอบงาน), was built 2026-08-19 and removed 2026-08-20** — confirmed redundant with the pre-existing Delivery Order module (FM-SL-05), per direct business-side confirmation; see [MODULES/Project.md](./MODULES/Project.md) "Work Handover Note — removed 2026-08-20". 28 `project`/`materialRequisition`/`jobOrder`/`purchaseRequest:*` permissions total, granted to Administrator/Super Admin only by default (none of the existing default roles belong to the Project/Store/Factory/Purchasing departments this module serves). History: [CHANGELOG.md](./CHANGELOG.md). | [MODULES/Project.md](./MODULES/Project.md) |
