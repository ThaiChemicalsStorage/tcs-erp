node_modules
dist
dist-ssr
*.local
.env
.env.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Build artifacts
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
.env*
.playwright-mcp/
.claude/

# .env* above must not swallow the committed template
!.env.example

# Docker — docker-compose.yml is deliberately untracked (owner request, 2026-08-06);
# a full reference copy lives in docs/DEPLOYMENT.md "Docker". Dockerfile and nginx/ ARE committed
# (real certs under nginx/certs/ are ignored by that folder's own .gitignore).
docker-compose.yml
